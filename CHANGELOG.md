# Changelog

Toutes les évolutions notables de ce dépôt doivent être documentées ici.

Le format suit une logique proche de Keep a Changelog. Les versions suivent le SemVer ;
la série `0.x` est une phase de **stabilisation** (l'API, le schéma de configuration et le
modèle de données peuvent évoluer sans garantie de rétrocompatibilité jusqu'à `1.0.0`).

## [Unreleased]

### Added
- **Chat d'affinage des livrables** (demande utilisateurs) : sur la page résultats d'un job
  **terminé** (tous profils), un panneau de conversation avec la LLM locale — **Discuter**
  (question/vérification/proposition, aucun fichier modifié) puis **Appliquer** (la LLM édite
  les artefacts TEXTE — synthèse, SRT, données structurées, options de rendu — dans un
  `AgentWorkspace` isolé, sous garde-fous déterministes : intégrité SRT réutilisée, JSON
  normalisé, options filtrées). Chaque application crée une **version restaurable**
  (`refine/versions/v<N>/`, bouton Restaurer) ; les documents (DOCX/ZIP) sont régénérés.
  La conversation a du **contexte** (les derniers tours sont rejoués à la LLM). Chaque tour
  transite par la **file** (mode d'étape `refine` → même admission VRAM/verrou LLM que les
  jobs ; occupé ⇒ tour assistant explicite, jamais d'attente bloquante). Le DOCX n'est JAMAIS
  édité en binaire : il reste un rendu déterministe des artefacts texte.
- **Options de rendu du DOCX pilotables** (`context/render_options.json`) : thème (clés de
  `_THEMES`, prime sur le type de réunion) + sections on/off (participants / transcription /
  points à vérifier) avec renumérotation séquentielle — modifiables par la LLM (mode apply)
  OU directement depuis l'UI **sans LLM** (`POST /api/jobs/<id>/refine/render-options`,
  effet immédiat, versionné). Tout invalide est ignoré : le rendu ne casse jamais.
- **API** : `POST /api/jobs/<id>/refine` (kind discuss/apply, RBAC propriétaire/admin, 409 si
  demande active, 202 accepté), `GET /api/jobs/<id>/refine/chat` (polling unique : tours,
  busy, versions, options), `POST /api/jobs/<id>/refine/revert` (restauration de version).
  Nouveaux prompts `configs/prompts/refine_{discuss,apply}_prompt.txt` (placeholders
  abstraits) ; config `workflow.refine_chat.*` ; actions d'audit dédiées ; préfixe `refine/`
  synchronisé (topologie split). Oracle UI Playwright : 2 checks GPU-free (31/31 en réel).

## [0.1.0-beta.7] — 2026-07-02

> **Feature phare : gestion multi-backend de la LLM d'arbitrage (Ollama / llama.cpp / vLLM),
> pilotée par un catalogue de données unique et par le matériel.** Ollama devient le backend
> « facile » (installation `curl | sh`, sans compilation ni token) ; le choix « palier VRAM →
> modèle » sort du code vers `transcria/data/llm_profiles.yaml` (aucune taille en dur) ; la
> réservation VRAM est **dérivée de la taille réelle du modèle** puis recalée par la mesure au
> 1ᵉʳ load. Validé E2E réel sur 8× RTX 3090 (all-in-one Ollama mono/multi-GPU, llama.cpp, split
> vLLM) sur ubuntu2404/debian12/fedora41 — voir `docs/LLM_PROFILS_VALIDATION.md`.

### Added
- **Backend LLM Ollama (défaut « facile » en all-in-one)** : `curl … | sh` auto-suffisant
  (runtime CUDA embarqué → aucune compilation, aucun `nvcc`, aucun token HF), modèle du
  registre tiré par `ollama pull`. Phase d'installateur testée `installer/ollama_phase.py` +
  sous-commande `installer.cli ollama` ; `install.sh` propose le choix de backend en all-in-one
  (interactif ; llama.cpp reste le défaut non-interactif, non régressif ; flag `--llm-backend`).
  Garde : proposé seulement si `nvidia-smi` répond, jamais d'installation de driver déléguée.
- **Catalogue de profils LLM en DONNÉES** (`transcria/data/llm_profiles.yaml`) : source unique
  par moteur (llama.cpp / Ollama / vLLM) et par palier → identifiant du modèle + contexte
  variable + placement + dtype KV, **aucune taille en dur**. Sélection pilotée par le matériel
  `transcria/config/llm_profiles.select_profile` (mono → 1 carte ; multi ≥2 → multi-GPU activé :
  llama.cpp tensor-split, Ollama `OLLAMA_SCHED_SPREAD`, vLLM `TP` auto). Surchargeable.
- **Empreinte VRAM dérivée** (`transcria/gpu/llm_footprint.py`) : poids = taille RÉELLE du
  fichier + KV **calculé** (archi × contexte) ; **recalée par la mesure au 1ᵉʳ load** (Ollama
  `/api/ps`, `VRAMManager.recalibrate_llm_vram_from_measurement`) → `gpu.llm_vram_mb` correct.
- **Voie llama.cpp CUDA à 3 niveaux** : détecter → **binaire précompilé ai-dock** (opt-in,
  build épinglé, **sha256 vérifié**) → compiler si `nvcc` présent → échec propre. Le binaire
  ai-dock (b9851, CUDA 12.8) est **auto-téléchargé en non-interactif** si `llama-server` absent
  (validé E2E : install sur distro vierge sans compilation).
- **Résolveur vLLM data-driven** (`install_arbitrage --vllm-env`) : modèle/TP/max_len depuis le
  catalogue selon le matériel ; `launch_arbitrage_vllm.sh` résout ses défauts en best-effort.
- **Tests de paliers VRAM simulés (54 tests GPU-free)** : `tests/test_llm_paliers_simules.py`
  couvre `select_profile` × `recommend` sur tout l'univers de cartes NVIDIA (8→80 Go),
  mono/multi/hétérogène, cohérence catalogue↔placement, chemin transcription brute,
  empreinte dérivée, non-régression du catalogue. Reproductible en CI.
- **Option `--gpu-count N`** du harnais `verify_install_matrix.py` : limite les GPU via CDI
  (un `--device` par GPU) → simule un mono-GPU ou un petit multi-GPU sans toucher l'hôte.
- **Docs** : `docs/LLM_BACKENDS.md` (3 paradigmes de cycle de vie, VRAM dérivée, **recommandation
  backend par palier** : llama.cpp pour 12/16/24, Ollama/llama.cpp pour 48/64, vLLM pour le split),
  `docs/LLM_PROFILS_VALIDATION.md` (matrice + journal de validation E2E).

### Changed
- **Cycle de vie LLM unifié derrière `LLMBackend`** (`unload()` / `is_loaded()`) : `VRAMManager`
  **délègue** au backend au lieu de supposer « tuer un process ». Pour Ollama, la préemption
  VRAM STT↔LLM **décharge le modèle** (`/api/ps` + `keep_alive:0`) sans jamais tuer le démon
  persistant (exclu des kills agressifs). llama.cpp/vLLM : comportement inchangé.
- **`resolve_arbitrage_endpoint` backend-aware** : source unique (VRAMManager + provider
  opencode) suit `ollama_url`/11434 pour Ollama. `services.backend`/`ollama_url`/`ollama_model`
  documentés ; auto-détection rétro-compatible si absents.
- **`distro_bootstrap`** : ajout de `numactl`, `lsof`, `zstd` aux prérequis (lacunes réelles).

### Fixed
- **Calibration VRAM Ollama non dérivée** : `ollama_phase` n'écrivait pas `gpu.llm_vram_mb`
  → blocage `waiting_vram`. Correctif : `_measure_ollama_vram()` dérive l'empreinte de la
  taille réelle via `/api/tags`.
- **`detect_llama_server.py` SyntaxError** : f-string apostrophes imbriquées → guillemets doubles.
- **`install.sh` non-interactif** : GGUF + binaire ai-dock non téléchargés (`ask_yn` = false)
  → téléchargement automatique + binaire ai-dock b9851 (sha256 vérifié).
- **Profil `64gb` en conteneur** : `numactl` interdit par seccomp → conditionnel ;
  `CUDA_HOME` hardcodé → conditionnel ; `LD_LIBRARY_PATH` libs nvidia du venv torch ajoutées.
- **`distro_bootstrap.py`** : `DEBIAN_FRONTEND` non propagé → prefixé dans `install_template` ;
  ubuntu2204 Python 3.10 → PPA deadsnakes `python3.11` ; rocky9 conflit `curl-minimal` → `--allowerasing`.
- **`install.sh` détection `PYTHON_BIN` trop tardive** → déplacée avant les appels aux modules
  Python 3.11+ (Rocky 9 a `python3` = 3.9 système).
- **CDI multi-GPU** : `nvidia.com/gpu=0,1` non supporté par Docker 29 → un `--device` par GPU.

## [0.1.0-beta.6] — 2026-06-27

### Added
- **Image Docker all-in-one GPU — workflow complet en une commande, sans token** (+ publiable).
  Nouvelle image dédiée `Dockerfile.allinone-gpu` (base **CUDA 12.6** ; cu128 impossible car le
  projet épingle `torchcodec>=0.12`, publié sur cu126/cu130 seulement) qui **compile `llama-server`
  CUDA** (llama.cpp ne publie pas de binaire CUDA Linux) → **LLM d'arbitrage embarquée**. Les
  défauts zéro-friction du quickstart sans `HF_TOKEN` deviennent **non gated** : STT `whisper` +
  diarisation **`sortformer`** (NVIDIA, ≤4 locuteurs) + petit GGUF d'arbitrage (palier 12 Go,
  `unsloth/Qwen3.5-9B-GGUF`, non gated, téléchargé au runtime dans le volume `models`) → les 6
  profils tournent sans aucun token ; un token HF gratuit (+ conditions Cohere **et** pyannote)
  bascule sur la qualité de référence (locuteurs illimités). **Aucun poids n'est baké** (build
  hermétique) ⇒ image **publiable** : workflow `.github/workflows/publish-image.yml` (GHCR,
  CUDA 12.6 figée, driver ≥ 535), et `TRANSCRIA_ALLINONE_IMAGE=ghcr.io/<owner>/…` fait un `pull`
  au lieu d'un build. Entrypoint : `provision_arbitrage_model` (téléchargement idempotent + mode
  `--provision-only` pour pré-fetch) ; lancement LLM in-container via l'autonomie VRAM existante
  (`TRANSCRIA_ARBITRAGE_SCRIPT` déduit du palier). Compose : service `migrate-gpu` (même image GPU).
  Détails : `docs/DOCKER.md` § All-in-one GPU / Publication.
  **Validé E2E réel sur `test2.mp3`** (8× RTX 3090) : (a) qualité de référence cohere+pyannote
  → 98/100 ; (b) **chemin zéro-token** whisper + Sortformer (téléchargés sans token) + LLM
  embarquée (llama-server lancé, opencode exit 0, corrections réelles) → 74/100, livrables
  SRT/ZIP/DOCX. Piège documenté : un cache HF hôte pré-rempli par un autre utilisateur peut
  casser faster-whisper (`File exists`) — cache vierge OK.
  **Prérequis GPU/VRAM documentés** (`docs/DOCKER.md`, table de compatibilité par génération) :
  GPU compute ≥ 7.5 (Turing) **et ≥ 12 Go** ; SASS sm_75→90 + **PTX sm_90 → JIT** vers Blackwell
  (RTX 50xx/B100/B200, 1er run plus lent) ; Pascal/Volta (< 7.5) exclus ; driver ≥ 525.
  VRAM **non additive** (autonomie load/unload) →
  un seul GPU **~12 Go** suffit (pic = LLM 9B ~10,6 Go ; whisper < 5 Go, sortformer ~3,5 Go,
  séquencés). Le quickstart **aligne `gpu.llm_vram_mb` sur le palier** (12 → 12000) — sinon le
  défaut 60000 (palier 64 Go) faisait refuser l'admission du 9B sur une carte 12-24 Go.
- **Image Docker `:bundled` — modèles embarqués (zéro-download, hors-ligne).** Nouvelle image
  `Dockerfile.allinone-bundled` (variante de l'all-in-one GPU) qui **bake les modèles par défaut
  NON gated** : whisper large-v3 (MIT), Sortformer 4spk (NVIDIA Open Model License), Qwen3.5-9B
  Q5_K_M (Apache-2.0) **et** le modèle de qualité audio SQUIM (torchaudio, ~29 Mo, seul modèle
  encore téléchargé au runtime — désormais baké ⇒ **aucun download au 1ᵉ run**, validé E2E). Couche modèles **avant `COPY . /app`** (un patch de code ne réinvalide pas
  les 12,5 Go). Le mode `--bundled` du quickstart monte le cache HF en **volume nommé `hfcache`
  seedé depuis l'image** (`TRANSCRIA_HF_SOURCE=hfcache`) au lieu du bind du cache hôte → **supprime
  le piège `[Errno 17] File exists`**. Licences de redistribution **vérifiées** ; attributions
  bakées dans **`/licenses/`** (NOTICE + texte intégral de la NVIDIA Open Model License + MIT). Ce
  n'est **pas** « full » : pyannote/Cohere et les paliers LLM > 12 Go restent en opt-in
  (`HF_TOKEN` / `TRANSCRIA_LLM_TIER`). Publication = **build local + push manuel** (~31 Go > disque
  runner CI). Détails : `docs/DOCKER.md` § Image `:bundled`.
- **Preflight GPU du quickstart** (`transcria.deploy.gpu_preflight`) : vérifie **compute capability
  ≥ 7.5 ET VRAM ≥ ~12 Go** via `nvidia-smi` AVANT le build/pull → échec tôt avec message
  actionnable (carte non supportée / VRAM insuffisante) au lieu d'un crash CUDA au 1ᵉ job.

## [0.1.0-beta.5] — 2026-06-26

### Added
- **Profils de traitement** (remplacent le binaire `fast`/`quality`) — jalon vers la 1.0, sur
  retour utilisateurs (le choix binaire bloquait l'adoption). 6 profils nommés (`srt_express`,
  `srt_locuteurs`, `word_rapide`, `word_structure`, `word_corrige`, `dossier_qualite`) + `legacy_fast`
  transitoire. Après l'upload, l'utilisateur choisit un *livrable* sur un curseur ; le portail grise
  les profils non lançables (raison affichée) et présélectionne le plus complet que la config/le
  matériel valident. Implémenté de bout en bout : modèle central (`transcria/workflow/profiles.py`),
  disponibilité backend (`profile_availability.py` + `GET /api/profiles/availability`), circulation
  de `processing_profile_id` (API/exécution via `extra_data.execution`, sans migration), estimation
  VRAM par profil (`estimate_profile_resources` → un profil léger n'est pas bloqué par les ressources
  qu'il n'utilise pas), pipeline piloté par le profil (parité stricte : `dossier_qualite` == ancien
  `quality`, `legacy_fast` == ancien `fast`), exports/qualité gradués (`zip_level`/`docx_level`,
  contrôle qualité léger `quality/light_report.py`). Compatibilité ascendante : un job sans profil
  retombe sur le comportement complet. Cadrage : `docs/PROFILS_TRAITEMENT_WORKFLOW.md`. Restent la
  validation de charge mixte par profil, la granularité du *contenu* DOCX par niveau, la politique
  `api_quality` et des notifications profile-aware.

### Fixed
- **Phases LLM (correction / relecture / résumé) suspendues en mode headless.** `opencode run`
  est non-interactif : la permission `external_directory` (défaut opencode `ask`) se déclenchait
  quand un agent explorait son scratch via `glob`/`grep` (ces outils remontent au dossier parent du
  scratch) ; sans répondeur, l'`ask` suspendait le run → sortie jamais écrite, échec « sans production »
  intermittent (observé sur la phase correction en topologie split). Correctif : `ensure_agent_permissions`
  pose dans `opencode.json` une politique `external_directory` déterministe (`allow` sur l'arbre de
  travail des agents `resolve_agent_work_root`, `deny` ailleurs, jamais `ask`), appliquée aux deux sites
  de provisioning (hôte `scripts/setup_opencode.py`, Docker `deploy/entrypoint.provision_opencode`).
  Règle : en headless, aucune permission opencode ne doit valoir `ask`. Cf. AGENTS.md (AgentWorkspace).
- **STT distant : la transcription rapide du résumé ne lançait pas le moteur à la demande.** Le
  pipeline principal demande au nœud d'assurer le moteur STT (`/engines/ensure`), mais la
  transcription rapide du **résumé** tourne hors de ce pipeline → sur un nœud de ressources frais,
  le moteur cohere n'était jamais lancé et le STT échouait en « connection refused » sans fallback
  (l'utilisateur restait bloqué). `runner._preflight_remote_stt` exécute désormais le même pré-vol
  (admission + `/engines/ensure`, qui attend que le moteur soit sain) avant la transcription rapide
  distante ; un moteur en préparation diffère le job (re-queue), un nœud durablement indisponible
  échoue proprement.
- **Démarrage migrate : « base injoignable » masquait une erreur d'AUTHENTIFICATION.** Quand un
  volume PostgreSQL préexistant a été initialisé avec un autre `POSTGRES_PASSWORD`, l'auth TCP
  échoue alors que le service répond — l'ancien message générique « injoignable » envoyait
  l'opérateur sur une fausse piste réseau. `entrypoint.classify_db_unreachable` rejoue une
  connexion à l'échec et distingue auth / DNS / connexion refusée, avec une remédiation explicite
  (réutiliser le mot de passe d'origine, `down -v`, ou `ALTER USER … PASSWORD`).

### Changed
- **Audit licences / mentions tierces (préalable à une éventuelle image publique).**
  `THIRD_PARTY_NOTICES.md` complété : composants embarqués dans les images Docker (opencode — MIT ;
  ffmpeg — GPL/LGPL via Debian, redistribué inchangé, sources Debian disponibles), copyleft faible
  runtime explicité (`lameenc` LGPLv3, `certifi` MPL-2.0, `text-unidecode` Artistic), lot Apache-2.0
  (transformers/vLLM/NeMo/HF — NOTICE conservé), et modèles téléchargés au runtime (Cohere/pyannote/
  whisper/Qwen — non redistribués, *gated*). **Aucune dépendance GPL/AGPL au runtime** (le code reste
  Apache-2.0 sans contamination ; `pytest-postgresql` LGPLv3 est dev-only, hors images). Pointeur
  ajouté dans la section *License* du README.

## [0.1.0-beta.4] — 2026-06-23

**Durcissement de la concurrence + validation all-in-one Docker.** Re-test des trois topologies
Docker sur installation neuve, puis **campagne de tests de charge (concurrence)** : robustesse validée
(all-in-one 3 jobs ; split jusqu'à 8 jobs, 0 échec serveur, débit qui scale jusqu'au sweet spot ≈ 4 sur
8×RTX 3090). **7 vrais bugs débusqués et corrigés** « comme un utilisateur » (4 de concurrence + 3 du
chemin all-in-one Docker, ci-dessous). Gate : ruff + mypy (arbre), **2 563 tests** verts (~80 %
couverture). Phase `0.x` : l'API, le schéma de configuration et le modèle de données peuvent encore
évoluer jusqu'à `1.0.0`.

**Tests de charge (concurrence) des topologies — `docs/PLAN_TEST_CHARGE.md`.** Campagne de stress
(rafales `test2.mp3`) : all-in-one robuste à 3 jobs ; split robuste **jusqu'à 8 jobs** (0 échec serveur),
débit qui scale 1→4 puis le LLM 27B sature (sweet spot ≈ 4 sur 8×RTX 3090). **4 vrais bugs de
concurrence corrigés** :

### Added
- `scripts/load_test.py` (générateur de charge : N clients en rafale, récap débit/p50/p95) et
  `scripts/load_sampler.py` (échantillonneur GPU / `/capabilities` / métriques vLLM).
- **Capacité d'admission du nœud configurable** : `resource_node.max_concurrent_jobs` (défaut 1),
  annoncée dans `/capabilities`, exploitée par le scheduler — lève le plafond « 1 job » du split.

### Fixed
- **Diarisation : `Cannot copy out of meta tensor` sous concurrence.** `transformers
  from_pretrained(device_map=…)` déclenche `accelerate.init_empty_weights()` (monkeypatch meta GLOBAL
  non thread-safe) qui contamine l'instanciation concurrente de pyannote. → **verrou global
  d'instanciation** `transcria.gpu.model_load_lock` autour des chargements de modèles.
- **opencode se fige sous concurrence.** Base SQLite `opencode.db` partagée entre invocations
  concurrentes → contention du writer. → **`XDG_DATA_HOME` par invocation** (une db par run).
- **LLM d'arbitrage distante sérialisée à tort.** Le verrou LLM de l'allocator (modèle « LLM locale
  mono-GPU ») sérialisait l'accès même à un vLLM distant qui batche → `correction` échouait. → **verrou
  LLM no-op si distant** ; `vram_manager.stop_arbitrage_llm` **no-op si distant** ; admission découplée
  des moteurs in-process sérialisés (diar/voice-embed s'auto-sérialisent, ne bornent plus l'admission).
- **Health-check de la LLM distante faux-négatif sous saturation.** Le probe faisait un test-inférence
  (`/v1/completions`, 60 s) qui se met en file derrière les requêtes réelles et timeout → faux « LLM non
  disponible » + échec dur de `correction`. → pour une LLM **distante**, `/v1/models` = preuve de vie
  suffisante (plus de test-inférence) ; `correction` **gracieuse** (`vram_wait`/re-queue) si la LLM
  distante est transitoirement indisponible (dégradation, pas crash). Local inchangé.

**Re-test de la topologie all-in-one Docker de bout en bout (mêmes correctifs « comme un
utilisateur »).** Le pipeline complet a été rejoué dans le conteneur all-in-one (STT Cohere +
diarisation pyannote auto-placée dans le conteneur, LLM d'arbitrage servie hors image) →
**qualité 97/100, identique au banc de référence**, livrables SRT/ZIP/DOCX. Trois correctifs de
fond pour que les **trois** topologies (all-in-one, frontale, nœud de ressources GPU) fonctionnent
sans bricolage.

### Fixed
- **opencode absent de l'image Docker de base (`transcria:latest`).** Cette image est construite par
  `pip install` (et non par `install.sh`) ; le correctif R9 de beta.3 ne couvrait que les images
  `Dockerfile.worker`/`Dockerfile.resource-node`. Les rôles `scheduler` (compose de base) et `all`
  (all-in-one) exécutent pourtant les phases LLM via opencode → elles échouaient faute de binaire.
  L'image de base installe désormais opencode via l'installateur officiel (comme `install.sh`),
  avec `HOME`/`PATH` du compte de service.
- **`provision_opencode` ignorait `TRANSCRIA_ARBITRAGE_LLM_HOST`.** `vram_manager` (sonde/cycle de vie)
  honore cette variable, mais `opencode_setup.default_base_url` (URL du provider opencode) lisait
  seulement `services.arbitrage_llm_host` → les deux **divergeaient** quand l'hôte d'arbitrage était
  fixé par l'environnement (opencode pointait dans le vide). Résolution désormais **unique et partagée**
  (`opencode_setup.resolve_arbitrage_endpoint` : env > config > `127.0.0.1`), utilisée par les deux.
- **`scripts/verify_split_topology.py` : applicable aux trois topologies.** Les appels `/summary` et
  `/process` sont **synchrones** en rôle `all` (réponse en fin de phase LLM) et enfilés (202) en split :
  le timeout fixe de 60 s (calibré pour l'async) provoquait un faux échec en all-in-one → timeout aligné
  sur le plafond du job. Le plan de contrôle (`--node`/`--arbitrage`) est désormais sautable (vide) pour
  l'all-in-one, qui n'a pas de nœud `:8002`.

## [0.1.0-beta.3] — 2026-06-23

**Topologie répartie (frontale + nœud de ressources) entièrement containerisée et validée de bout
en bout sur fichier son réel.** Banc 8× RTX 3090 : frontale CPU (web + scheduler) → **STT Cohere via
vLLM** + **diarisation pyannote auto-placée sur une carte libre** + **LLM d'arbitrage Qwen3.6-27B-FP8
via vLLM** (tensor-parallel 4, FP8 Marlin sur Ampere) — pipeline complet (transcription, diarisation,
résumé, correction, relecture, exports) produisant SRT/ZIP/DOCX. L'exercice « comme un utilisateur »
a débusqué et corrigé **14 problèmes** (dont plusieurs vrais bugs de fond, ci-dessous). Phase `0.x` :
l'API, le schéma de configuration et le modèle de données peuvent encore évoluer jusqu'à `1.0.0`.

### Added
- **Banc de test « topologie split GPU » entièrement containerisée** (`docker-compose.split-gpu.yml`
  + `config.split.example.yaml`) : frontale CPU + nœud de ressources GPU (diarisation pyannote +
  **STT Cohere via vLLM**) + **LLM d'arbitrage Qwen3.6-27B-FP8 via vLLM** (TP=4, FP8 Marlin), PostgreSQL
  partagé. Images dédiées `Dockerfile.worker` et `Dockerfile.resource-node` construites **en exécutant
  `install.sh`** (l'install est ainsi testée comme un utilisateur), le nœud ajoutant un venv vLLM isolé
  du venv projet (torch cu130). Nouveaux : `scripts/launch_arbitrage_vllm.sh` (lanceur arbitrage vLLM
  portable, flags tool-calling/reasoning Qwen3.6), `scripts/verify_split_topology.py` (vérification E2E :
  plan de contrôle + job son réel), `docs/PLAN_TEST_SPLIT_VLLM.md`.
- **Couverture E2E du profil d'install `resource-node`** (`test_install_resource_node_profile_e2e`) :
  exerce réellement `install.sh --profile resource-node --inference-service` (chemin jusqu'ici non couvert).

### Fixed
- **`install.sh` : nom d'utilisateur du mainteneur codé en dur comme défaut.** `SERVICE_USER="${USER:-admin_ia}"`
  ciblait silencieusement `admin_ia`/`/home/admin_ia` (utilisateur étranger) dès que `$USER` est vide
  (build Docker, cron, certains CI) → opencode et chemins installés au mauvais endroit. Défaut générique
  désormais : `${USER:-$(id -un … || root)}`.
- **LLM d'arbitrage distante ni lancée ni gérée localement.** `vram_manager` sondait/arrêtait/relançait
  la LLM en `127.0.0.1` en **ignorant `services.arbitrage_llm_host`** : en topologie répartie (LLM sur un
  nœud séparé), le worker la croyait absente, tentait un lancement local (échec) et l'admission du
  scheduler différait les jobs à l'infini. `vram_manager` honore désormais l'hôte configuré (sonde HTTP
  distante) et ne gère **pas** le cycle de vie d'une LLM distante (la consomme si saine). All-in-one local
  inchangé.
- **Diarisation : placement GPU figé sur `cuda:0`.** Le moteur de diarisation chargeait pyannote sur
  `cuda:0` (pas de placement « auto »), tombant sur le GPU du LLM en multi-GPU → OOM. Il résout maintenant
  `auto`/`cuda` générique vers la **carte la plus libre** (≥ VRAM requise) au moment du chargement (mêmes
  primitives que SQUIM) ; un index explicite reste respecté, repli CPU sinon.
- **`resource_node.engines[].gpu_mem` ignoré au lancement réel des moteurs STT.** Le superviseur ne
  transmettait pas `gpu_mem` au lanceur → le moteur réservait ~0.85×VRAM quelle que soit la config
  (l'admission, elle, utilisait `gpu_mem`). `gpu_mem` est désormais transmis (`STT_GPU_MEM`).
- **`opencode_runner` : échec dur si `opencode_bin` configuré non résolu.** Ajout d'un repli sur la
  découverte aux emplacements connus (`find_opencode_binary`) quand le binaire configuré ne résout pas
  (config générique, binaire déplacé) — opencode étant installé à un emplacement standard.
- **opencode absent de TOUTES les images Docker → phases LLM impossibles en conteneur** (all-in-one
  compris). Binaire installé **par `install.sh` au build** (image worker) + provider `local` **reconfiguré
  au démarrage** depuis la config montée (`transcria.deploy.entrypoint.provision_opencode`, rôles
  `all`/`scheduler`, best-effort/idempotent). **Corrige aussi l'all-in-one Docker**, sans toucher l'install hôte.

## [0.1.0-beta.2] — 2026-06-21

Cycle de durcissement de l'installation depuis `beta.1`, **validé de bout en bout sur 4
distributions Linux (Ubuntu 22.04/24.04, Debian 12, Fedora 42) × 3 versions Python
(3.11/3.12/3.13)** — apt + dnf, systemd et non-systemd, PostgreSQL 14/15/16 — avec le
pipeline complet STT (Cohere) + diarisation (pyannote) + LLM (résumé / correction / relecture)
au banc de référence (97/100). Phase `0.x` : l'API, le schéma de configuration et le modèle de
données peuvent encore évoluer sans garantie de rétrocompatibilité jusqu'à `1.0.0`.

### Fixed
- **Sélection LLM d'arbitrage qui se sautait silencieusement (régression de la fonte d'`install.sh`).** La phase opencode étant un sous-processus, elle persistait `opencode_bin` dans `config.yaml` mais ne pouvait pas réassigner la variable shell `OPENCODE_BIN` — restée vide → la SECTION 9-bis (palier VRAM, téléchargement GGUF, calibration) se croyait « opencode manquant » et se sautait sur toute machine GPU, même opencode installé (le `doctor` le voyait, masquant le bug). `install.sh` récupère désormais `OPENCODE_BIN` depuis `config.yaml` après la phase, avec validation que le binaire résout réellement. Garde structurel ajouté.
- **Faux « Cohere ASR ABSENT » à l'installation.** La détection ne regardait qu'un répertoire local, alors que `models.cohere_model_path` vaut par défaut un repo id (`CohereLabs/cohere-transcribe-03-2026`) dont les poids vivent dans le cache HF. La détection scrute maintenant aussi le cache HF par repo id (comme pyannote) — fin du faux négatif.
- **`opencode.json` introuvable par doctor quand le service tourne sous un autre utilisateur.** La phase opencode écrit `opencode.json` dans le HOME du `SERVICE_USER` (correct : le service systemd y lit sa config), mais `doctor` résout le chemin via le `~` de l'appelant → faux négatif « provider/modèle non résolu » quand on lance doctor (ou la barrière post-install) sous un utilisateur ≠ `SERVICE_USER`. Correctif : `OPENCODE_CONFIG` (chemin absolu du `opencode.json` réellement écrit) est désormais (a) exporté pour la barrière doctor d'`install.sh` et (b) figé dans `.env` quand opencode est configuré — `start.sh` source `.env` et `doctor`/opencode honorent `OPENCODE_CONFIG`, donc la résolution ne dépend plus du HOME du process.
- **`torchcodec` (décodeur audio de pyannote.audio 4.x) non géré → diarisation cassée en conteneur.** `pyannote.audio>=4.0` exige `torchcodec` (AudioDecoder), absent de `requirements.txt` : il arrivait en transitif depuis PyPI, avec un wheel bâti pour un autre torch/CUDA que celui installé depuis l'index PyTorch → `AudioDecoder` indisponible, diarisation HS (constaté en conteneur). `torchcodec` est désormais installé **depuis le même index que torch** (install native + Dockerfile, comme torchaudio) — wheel apparié à l'ABI/CUDA de torch — et déclaré dans `requirements.txt` (`>=0.12,<1.0`, couplage torchcodec 0.12 ↔ torch 2.12). Le runtime fournit déjà les libs FFmpeg (`apt install ffmpeg`) requises par torchcodec.
- **Mot de passe PostgreSQL commençant par `-` rejeté à l'installation.** `install.sh` passait `--password "$pass"` aux sous-commandes `postgres` / `postgres-bootstrap` ; un mot de passe (généré ou choisi) débutant par `-` était lu comme une option par argparse → `error: argument --password: expected one argument` et install avortée. Passage à la forme `--password="$pass"`, robuste quelle que soit la valeur. (Corrige aussi une intermittence du filet E2E quand le mot de passe aléatoire commençait par `-`.)
- **Installation opencode cassée (404) — délégation à l'installateur officiel.** L'install téléchargeait un binaire « nu » `…/releases/latest/download/opencode-linux-x64`, mais l'asset GitHub est désormais une **archive** versionnée par cible (`opencode-linux-x64.tar.gz`, variantes `-musl` / `-baseline`) → le lien direct renvoie 404. On délègue maintenant à l'installateur officiel (`curl -fsSL https://opencode.ai/install | bash`, `HOME` forcé sur le répertoire cible) : il choisit le bon asset, l'architecture (x64/arm64), la libc musl et la **variante AVX2 baseline** — sans laquelle le binaire standard plante en *illegal instruction* sur un CPU/VM sans AVX2. Repli manuel mis à jour (commande officielle + npm/bun/brew). Doc `INSTALL.md` corrigée (les deux blocs au lien mort).
- **Plantage cryptique quand `python3-venv`/`ensurepip` manque.** Sur Debian/Ubuntu, `python3` est présent mais le module venv (`ensurepip`) est un paquet séparé : `python -m venv` échouait alors avec « ensurepip is not available », au milieu de l'install. `install.sh` vérifie maintenant en amont (`check-venv`) et s'arrête proprement avec `apt install python3-venv` avant le plantage (sauté en `--skip-deps`). `curl` (téléchargement opencode) est ajouté aux binaires optionnels signalés. Prérequis documentés dans `INSTALL.md`.

### Changed
- **`install.sh --non-interactive` installe opencode automatiquement** quand le profil requiert le LLM (auparavant : non installé faute de confirmation possible → phases LLM indisponibles). En interactif, la confirmation reste demandée ; un échec de téléchargement dégrade proprement (instructions manuelles, l'install ne plante pas).
- **Bootstrap PostgreSQL local idempotent.** Relancer `install.sh --postgres` sur une base déjà provisionnée tentait quand même le bootstrap privilégié (réécriture `pg_hba.conf`) → `PermissionError`. La phase sonde désormais d'abord la connexion applicative : si la base est joignable, tout le bootstrap privilégié est sauté (« déjà provisionnés »), sans appel `sudo`. Sinon, comportement inchangé.
- **Téléchargement Cohere via le CLI `hf` (fin de la dépendance au déprécié `huggingface-cli`).** La détection du CLI de téléchargement Cohere ne cherchait que `huggingface-cli` (déprécié) et utilisait `--local-dir-use-symlinks False` (supprimé des versions récentes / absent de `hf`). Préfère désormais `hf` (repli `huggingface-cli`) et abandonne le flag déprécié ; commandes manuelles d'`INSTALL.md` alignées sur `hf download`.

## [0.1.0-beta.1] — 2026-06-20

Première version taguée — **bêta**. Le produit est fonctionnel et couvert par 2 500+ tests
(CI verte : ruff, mypy, pytest complet sur PostgreSQL, couverture ~80 %), avec trois
topologies de déploiement (tout-en-un, frontale web + scheduler, nœud de ressources GPU)
et un déploiement conteneurisé validé de bout en bout sur GPU (cf. `docs/DOCKER.md`).
Évaluez/pilotez ; ne pariez pas la production sans votre propre validation.

L'ensemble des entrées ci-dessous (jusqu'à la précédente section non taguée) constitue le
contenu de cette première release.

### Fixed
- **Sonde de disponibilité LLM unifiée : « prêt » = modèle CHARGÉ et générant, pas « port ouvert ».** llama.cpp ouvre le port et répond à `/v1/models` **avant** d'avoir fini de charger le modèle (les complétions renvoient alors 503 « loading ») : un modèle volumineux à froid (ex. Q8 64 Go, ~30 s de chargement) pouvait être déclaré « prêt » alors que le 1ᵉʳ appel réel échouait. De plus, **deux sondes divergentes** coexistaient : `_port_utils.is_port_open` (la primitive partagée par `vram_manager` post-lancement / réutilisation et `llm_backend`) testait une complétion avec `max_tokens: 5` **et `text` seul** — le **piège des modèles reasoning** (premiers tokens dans `<think>` → `text` vide → faux négatif jusqu'au timeout, incident du 11/06/2026), alors que `ensure_arbitrage_llm_ready` avait, lui, une sonde correcte mais distincte. Unifié en une **source unique testée** : fonction **pure** `generation_confirmed(body)` (preuve de génération = texte **ou** `reasoning_content` **ou** ≥1 token généré, couvrant complétion/chat et modèles reasoning) ; `is_port_open` l'utilise avec un `max_tokens` sûr et traite un 503 comme « pas encore prêt » ; `ensure_arbitrage_llm_ready` réutilise la même fonction (fin de la divergence). 13 tests (`tests/test_port_utils.py`) dont le cas reasoning-seul et le 503-loading.

### Changed
- **Message d'échec du résumé différencié par CLASSE de panne (au lieu d'un message générique trompeur).** Quand le résumé LLM échoue, deux causes aux corrections OPPOSÉES étaient confondues sous « transcript trop long, modèle ou prompt » : (1) **erreur opencode** — opencode n'a pas pu appeler la LLM (modèle non résolu, serveur en erreur, binaire absent, timeout ; `success=False`) → diagnostic config/infra ; (2) **production vide** — opencode a fini (exit 0) mais n'a émis aucun texte → piste transcript/prompt. `OpenCodeRunner.run()` **récupère désormais le message de l'événement d'erreur opencode sur stdout** (ex. « UnknownError: Unexpected server error ») — auparavant l'erreur restait vide car elle n'est pas sur stderr. `run_summary` propage un `_failure_kind` (`opencode_error`/`empty_output`), porté jusqu'à la route via `summary_llm_error_kind` ; la route choisit le bon message — la classe `opencode_error` renvoie vers **`transcria doctor`** (résolution du modèle) plutôt que vers « raccourcir le transcript ». 3 tests (reproduction de l'événement d'erreur sur stdout + classification des deux classes).

### Added
- **Docker GPU : E2E réel validé + activation hôte reproductible + chemin testeur sans token.** Suite directe de la validation GPU : (1) **E2E complet en conteneur** sur `tests/test2.mp3` (`--mode quality --skip-llm`, HF_TOKEN + cache HF monté) — STT Cohere + diarisation pyannote **sur GPU** → 29 segments, 2 locuteurs, SRT 2630 c., score qualité 97/100 ; (2) `scripts/setup_docker_gpu.sh` — activation hôte **idempotente** de l'accès GPU Docker (installe `nvidia-container-toolkit` apt/dnf, génère la spec CDI, vérifie qu'un conteneur voit le GPU ; mode `--check`) — **volontairement hors `requirements.txt`** (deps Python) **et hors `install.sh`** (installateur natif), car c'est une config de l'hôte Docker ; (3) compose `all-in-one` câblé pour transcrire réellement (`HF_TOKEN`, cache `HF_CACHE_DIR`→`/hf`, `HF_HUB_OFFLINE=0` — `cohere_transcriber` force l'offline par défaut, à lever en conteneur pour la 1re résolution d'un modèle gated) ; (4) `docs/DOCKER.md` — section **Prérequis** : script GPU + matrice « test rapide whisper (non gated, sans token) vs Cohere (gated, token requis) », acceptation des conditions du modèle, et statut de validation E2E. Réponses aux deux frictions utilisateur : *« comment un testeur active le GPU »* (script dédié) et *« comment gérer le modèle gated »* (token documenté + alternative whisper sans token).
- **Déploiement Docker GPU validé + tout-en-un une commande (P5).** Le volet Docker passe de « CPU validé / GPU documenté » à **GPU validé de bout en bout**. Nouveau rôle **`all`** (tout-en-un : UI web + scheduler + inférence GPU in-process) dans `transcria/deploy/entrypoint.py` (commande `python app.py --role all --host … --port …`, PostgreSQL requis) — le plus simple pour qu'un utilisateur **teste** le projet dans un seul conteneur. Profil compose **`gpu`** : service `all-in-one` avec réservation GPU par **CDI** (`devices: ["nvidia.com/gpu=all"]`), `docker compose --profile gpu up` → db → migrate → all-in-one. `docs/DOCKER.md` complété avec les instructions GPU **réellement validées** : activation hôte (`nvidia-container-toolkit` + `nvidia-ctk cdi generate`), syntaxe CDI `--device nvidia.com/gpu=…` (le `--gpus all` échoue sur cet hôte → « AMD CDI spec not found »), build `TORCH_INDEX_URL=cu130`, et le caveat serveur de dev pour `all`. **Vérifié sur 8× RTX 3090 / driver 580 / Docker 29** : image CUDA (torch 2.12+cu130) `torch.cuda` + matmul GPU en conteneur ; `resource-node` (`/health` 200, `/capabilities` énumère la RTX 3090) ; tout-en-un GPU (`/health` 200 sur 7870, GPU vu). 3 tests unitaires supplémentaires pour le rôle `all` (commande, garde PostgreSQL). Non couvert : un job réel STT+diarisation+LLM de bout en bout (boot + accès GPU prouvés ; modèles/LLM = dépendances montées/externes).
- **Déploiement Docker (P5) — entrypoint par rôle + image multi-étages + compose + docs.** Socle de la conteneurisation, réutilisant les invariants des profils sans jamais lancer `install.sh` comme entrypoint applicatif. `transcria/deploy/entrypoint.py` (`python -m transcria.deploy.entrypoint <role>`) : rôles `web`/`scheduler`/`resource-node`/`migrate` avec les commandes fidèles aux unités systemd (`gunicorn wsgi:app`, `python app.py --role scheduler`, `gunicorn inference_service:create_app()`, `alembic upgrade head`) ; **PostgreSQL obligatoire** (SQLite refusé) pour les rôles à base applicative, `resource-node` (nœud GPU) sans base ; **`migrate` en job one-shot** (les serveurs n'auto-migrent pas) ; attente de la base au démarrage via la **sonde de la phase PostgreSQL** (SQLAlchemy/psycopg, sans `psql`) ; `exec` qui remplace le process (PID 1 hérite des signaux) ; sonde DB + exec injectables. `Dockerfile` multi-étages (venv builder → runtime mince ; `TORCH_INDEX_URL` CPU par défaut, override CUDA pour le nœud GPU), `.dockerignore` (ni venv/secrets/artefacts), `docker-compose.yml` (db *healthy* → `migrate` one-shot → `web`+`scheduler` ; volumes config/.env/jobs/models ; secrets par env) validé au niveau schéma (`docker compose config`). `docs/DOCKER.md` : schéma cible, matrice des variables, procédures de démarrage et de rollback, nœud de ressources GPU, LLM externe. 16 tests unitaires (`tests/test_deploy_entrypoint.py`). Le `docker build`/`compose up` réel (images PyTorch lourdes) reste l'étape de validation opérateur.

### Changed
- **Installateur Python `transcria.installer` : résumé final fondu — `install.sh` clos (plus aucune logique ni rendu métier).** La SECTION 12 (en-tête du profil, bilan des modèles IA, base de données, valeurs `CHANGE-ME` restantes, commandes de démarrage) passe dans `summary_phase.apply_summary(plan)` : une seule invocation `python -m transcria.installer.cli summary` au lieu des ~6 sous-processus de rendu que le shell enchaînait, le décompte des `CHANGE-ME` étant fait en process (`count_text_occurrences`). Réutilise les renderers déjà testés (`install_profiles.render_profile_summary_text`/`render_profile_next_steps_text`, `install_models.render_model_summary`, `install_summary.render_database_summary`/`render_configuration_summary`). Nouvelle méthode `Console.write()` (texte verbatim, sans tag). Wrappers shell morts supprimés (`print_profile_text`, `print_model_summary`, `print_database_summary`, `print_configuration_summary`). 5 tests unitaires (profils web/resource-node, décompte CHANGE-ME, config absente, drapeaux LLM) ; filet E2E vert (résumé exercé de bout en bout) ; 4 tests structurels mis à jour. **Bilan du chantier** : `install.sh` est passé de ~1728 à ~1180 lignes et ne contient plus que le bootstrap (prérequis, interpréteur, venv), l'orchestration interactive et les délégations vers les 8 sous-commandes de `transcria.installer.cli` (python-env, config, config-proxy, opencode, postgres, postgres-bootstrap, systemd, summary) — toute la logique métier vit dans des modules Python testés à dépendances injectables.
- **Installateur Python `transcria.installer` : bootstrap PostgreSQL local privilégié fondu — dernière logique métier sortie du shell.** Le provisionnement d'une PostgreSQL *locale* (réécriture de `pg_hba.conf` pour l'auth TCP par mot de passe, création idempotente du rôle et de la base — UTF8 imposé via `template0`, repli locale C —, reload du service) passe dans `postgres_phase.apply_postgres_bootstrap(plan)`. Tout transite par l'identité système `postgres` (`sudo -u postgres` / `runuser`), passée en **préfixes injectables** (`admin_psql`, `admin_pg_hba_rewrite`, `reload_service`) ; le SQL réutilise les renderers audités de `transcria.install_postgres` (`render_role_sql`, `render_database_sql`, `rewrite_pg_hba_file`, `render_pg_hba_rewrite_result`). `install.sh` délègue via `python -m transcria.installer.cli postgres-bootstrap` ; helpers shell morts supprimés (`pg_admin_psql`, `pg_admin_python_module`, `pg_state_query`). **Couverture** : ce chemin n'est pas exercé par le filet E2E (qui utilise `--pg-existing`), mais 9 tests unitaires couvrent l'orchestration (pg_hba changé/inchangé/échec/invalide, rôle ok/ko, base existante/créée/repli-C/double-échec) **et un test d'intégration crée réellement le rôle + la base sur le cluster PostgreSQL éphémère** (admin_psql injecté en superuser) — retirant la dépendance à un harnais privilégié. ⇒ toute la logique métier de `install.sh` est désormais fondue dans `transcria.installer` ; ne restent en shell que le bootstrap (prérequis, choix interpréteur, activation venv), les invites interactives et le résumé final. 4 tests structurels mis à jour.
- **Installateur Python `transcria.installer` : bloc proxy d'entreprise fondu (fin de la SECTION 6).** La persistance du proxy dans `.env` (détecté côté shell, indispensable car le service systemd n'hérite pas de l'environnement du shell → sinon les téléchargements de modèles *pendent*) passe dans `config_phase.py` (`apply_proxy(plan)`). Le **gate** reste en shell (il lit l'environnement de l'installateur : `https_proxy`/`HTTPS_PROXY`/…) ; la **décision** (déjà-présent via `has_any_env_key`, confirmation interactive, écriture des 3 variables + `chown` best-effort vers l'utilisateur de service) est déléguée. Non-interactif → persiste (fidèle à `ask_yn`, non appelé dans ce mode) ; confirmation et chown injectables. `install.sh` délègue via `python -m transcria.installer.cli config-proxy` ; plus aucun `has-any`/`env_set`/`ask_yn` inline pour le proxy. 5 tests unitaires (présent/non-interactif/confirmé/refusé/chown-root) ; filet E2E vert ; 1 test structurel mis à jour. Dernière mécanique `.env` interactive sortie du shell — l'écriture de `.env` est désormais entièrement portée par Python.
- **Installateur Python `transcria.installer` : 5ᵉ phase fondue — services systemd.** L'orchestration de la SECTION 11 d'`install.sh` (construction du plan d'unités du profil, avertissement legacy en déploiement split, préparation/chown des répertoires de logs, rendu depuis les templates versionnés, installation privilégiée — copie + `daemon-reload` + `enable`, ou écriture d'un fichier `.adapted` quand `sudo` manque) passe dans `transcria/installer/systemd_phase.py` (`apply_systemd(plan)`). La logique métier (plan, rendu, `install_rendered_unit`) vit déjà dans `transcria.install_systemd` (fonctions pures, runner injectable) : la phase l'appelle **en process** au lieu de la piloter via des lignes `|` reparsées par le shell (fin de la boucle `--unit-plan`/`--kind`/`--install-unit`). Opérations système toutes **injectables** (systemctl `is-enabled`, chown récursif, existence d'utilisateur, création de répertoires via `transcria.install_paths`). La section « Services systemd » n'est affichée que si le plan n'est pas vide (fidèle au shell). `install.sh` délègue via `python -m transcria.installer.cli systemd` (python du venv) en transmettant `--have-sudo`/`--have-systemctl`/profil. Helper shell mort `log_systemd_event` supprimé. 8 tests unitaires (systemctl/chown/user/runner injectés, branches sudo et `.adapted`, sans systemd ni `/etc`) ; filet E2E vert (exerce le câblage avec `--no-service` → plan vide) ; 3 tests structurels `install.sh` mis à jour.
- **Installateur Python `transcria.installer` : 4ᵉ phase fondue — base PostgreSQL (chemin post-connexion).** La queue de `_setup_postgres` (SECTION 6.5 d'`install.sh` : test de connexion, garde d'encodage UTF8, écriture du DSN dans `.env`, détection d'état schéma/données/version Alembic, décision + exécution Alembic — `keep`/`upgrade-existing`/`create` avec reconstruction locale privilégiée en repli —, et migration SQLite→PostgreSQL) passe dans `transcria/installer/postgres_phase.py` (`apply_postgres(plan)`). **Choix tourné Docker** : la détection d'état et le test de connexion passent par **SQLAlchemy/psycopg** (dépendance dure de l'app) au lieu du client `psql` — le chemin « base existante / distante » n'exige donc plus le binaire `psql` (seul le bootstrap local, resté en shell, en dépend). Dépendances injectables : lectures DB (`query`), `alembic_upgrade`, `admin_psql` (DROP/CREATE SCHEMA privilégié du rebuild local), `migrate` (réutilise `run_sqlite_migration`), `confirm` interactif, `chown`. Messages repris au mot près de `transcria.install_postgres` (texte audité). Le **bootstrap local privilégié** (réécriture `pg_hba.conf`, création rôle/base via `sudo -u postgres`, reload du service) reste volontairement en shell — identité système, non couvert par le filet E2E ; `install.sh` délègue le reste via `python -m transcria.installer.cli postgres` (python du venv, import différé) en passant l'identité psql privilégiée pour le rebuild (`--admin-psql`). Helpers shell morts supprimés (`pg_app_psql`, `build_pg_dsn`, `_do_pg_migrate`, les nested `log_postgres_alembic_event`/`log_sqlite_migration_event`/`log_postgres_schema_action_event`). **Chemin entièrement exercé E2E** via `--pg-existing` (profils web/scheduler) + 10 tests unitaires (DB/alembic/psql-privilégié/migration/prompt injectés, sans base réelle) ; tests structurels `install.sh` mis à jour (délégation + « plus aucune mécanique inline »).
- **Installateur Python `transcria.installer` : 3ᵉ phase fondue — opencode (détection / installation / configuration du provider).** La SECTION 9 d'`install.sh` passe dans `transcria/installer/opencode_phase.py` (`apply_opencode(plan)`) : détection du binaire, installation interactive si absent, configuration du provider local via `setup_opencode.py`. Réutilise en process les primitives de `transcria.install_opencode` (détection, install réseau, PATH, `chown` best-effort) + son texte audité ; dépendances injectables (runner sous-processus, `confirm` interactif, `chown`, **`detect`** — ce dernier rendant la phase indépendante du PATH réel de la machine). `install.sh` délègue via `python -m transcria.installer.cli opencode` (sous le python du venv, import différé). Helpers shell morts `opencode_helper` et `log_opencode_setup_event` supprimés. Le filet E2E force désormais `HOME=<sandbox>` pour que la config provider s'écrive dans le bac à sable, jamais dans le `~/.config/opencode` réel (étanchéité). 5 tests unitaires (réseau/sous-processus/prompt/chown injectés) ; 4 tests structurels de `install.sh` consolidés.
- **Installateur Python `transcria.installer` : 2ᵉ phase fondue — configuration (config.yaml + .env + secrets + rôle runtime).** Le cœur déterministe de la SECTION 6 d'`install.sh` (génération `config.yaml` via `bootstrap_config.py` avec sauvegarde si `--force`, init `.env`, `TRANSCRIA_SECRET`, `runtime.role`/`TRANSCRIA_ROLE`, `TRANSCRIA_INFERENCE_API_KEY`) passe dans `transcria/installer/config_phase.py` (`apply_config(plan)`, runner injectable). `install.sh` délègue via `python -m transcria.installer.cli config` lancé **sous le python du venv** (PyYAML + dépendances projet) ; le `cli` importe la phase en **lazy import** pour que la phase `python-env` (pré-venv, interpréteur système) ne charge jamais PyYAML. Le bloc proxy d'entreprise (interactif + `chmod`/`chown` privilégié) reste volontairement en shell. Messages repris au mot près de `transcria.install_summary` (texte audité). Helper shell mort `env_ensure_secret` supprimé. Comportement préservé (dont l'écriture du rôle même sur un `config.yaml` conservé). 5 tests unitaires (`tests/test_installer_config_phase.py`, bootstrap simulé par runner injecté) + filet E2E vert + 3 tests structurels de `install.sh` mis à jour.
- **Installateur Python `transcria.installer` : début de la fonte de `install.sh` (P2/§3.2) — 1ʳᵉ tranche = environnement Python (venv + PyTorch + dépendances).** `install.sh` portait encore l'orchestration des SECTIONS 2-4 (création venv, `pip --upgrade`, plan PyTorch + `pip install` CPU/CUDA, `requirements.txt`) en shell. Nouveau package `transcria/installer/` (`console.py` : rendu `[OK]/[INFO]/[WARN]/[ERROR]` fidèle au shell, ANSI auto-désactivé hors TTY ; `python_env.py` : `apply_python_env(plan)` à **runner de sous-processus injectable** ; `cli.py` : `python -m transcria.installer.cli python-env …`). `install.sh` ne garde que le bootstrap minimal (choix de l'interpréteur système + activation du venv produit) et délègue la phase. **Comportement préservé à l'identique** : le venv est créé avec l'interpréteur **système** (`PYTHON_BIN` n'est jamais re-pointé vers le venv dans `install.sh`), la détection PyTorch s'appuie donc sur ce même interpréteur (via `install_torch.build_install_plan`), et les `pip` ciblent explicitement le `python` du venv (équivalent du `pip` activé). Vérifié de bout en bout par le filet E2E (`install.sh --profile web|scheduler` toujours vert) + 15 tests unitaires sans réseau ni venv réel (`tests/test_installer_python_env.py`, `tests/test_installer_console.py`) couvrant skip-deps (venv requis), venv neuf/réutilisé, PyTorch CPU/CUDA/déjà-installé/skip, requirements. 4 tests structurels de `install.sh` mis à jour (ils épinglaient l'implémentation shell désormais déléguée).

### Added
- **Filet d'intégration E2E de l'install par profil + deux primitives alignées Docker (`--skip-deps`, `--pg-existing`).** La refonte `install:` (15 modules Python, ~5000 lignes de tests) testait exhaustivement les *briques* et le rendu `--plan`, mais **aucun test n'exécutait une install assemblée** : personne n'avait prouvé que `install.sh --profile web` produit une frontale qui démarre. Nouveau `tests/test_install_e2e.py` : exécute *réellement* `install.sh --profile web|scheduler` dans un bac à sable (venv + package symlinkés ; `scripts/`, `configs/`, `alembic/`, templates **copiés** pour interdire toute écriture vers le dépôt) contre une PostgreSQL éphémère, puis vérifie `config.yaml`/`.env` générés, DSN écrit, Alembic appliqué, `doctor --profile` vert, et **aucune fuite** vers le dépôt versionné (diff porcelaine avant/après). Borné par conception (étapes lourdes/privilégiées désactivées), il a immédiatement révélé deux manques d'intégration — comblés en même temps, et tous deux **alignés Docker** : (1) `--skip-deps` — environnement Python déjà provisionné (couche build Docker, venv existant) : ne crée pas de venv, ne lance aucun `pip` (implique `--no-torch`) ; (2) `--pg-existing` — rôle et base déjà provisionnés (PostgreSQL conteneur/service séparé, base distante, profil `migrate`) : écrit le DSN + applique Alembic **sans bootstrap privilégié** via `sudo postgres` (et lève la garde `sudo-missing` qui sinon avorte l'install en environnement non privilégié). Le harnais est paramétré par profil pour servir de socle aux futurs smokes de build Docker (P5). 2 contrats `--plan` supplémentaires (`tests/test_install_script.py`) verrouillent l'acceptation des deux flags sans PostgreSQL.
- **`transcria doctor` : contrôle statique de résolution du modèle opencode (GPU-free) — attrape la panne « aucun texte produit » à l'install, plus au 1ᵉʳ résumé.** Le pipeline lance `opencode run --model <workflow.arbitration_llm.model_id>` (ex. `local/arbitrage`) ; si le provider opencode n'expose **aucune clé de modèle** portant ce nom, l'appel échoue silencieusement par « aucun texte produit après 3 tentatives » — diagnostiqué seulement en prod (incident du 16/06/2026 : la config opencode du service keyait le modèle `qwen3-35b-arbitrage` alors que le pipeline demandait `arbitrage`, après une config écrite à la main au lieu de `setup_opencode.py`). Le smoke `--llm-smoke` l'aurait vu mais il est opt-in, démarre la LLM et consomme de la VRAM. Nouveau `check_opencode_model_resolution` : **lit statiquement** `~/.config/opencode/opencode.json` (résolution `OPENCODE_CONFIG`/XDG) et vérifie que `provider.<provider>.models` contient la clé issue du `model_id` de `config.yaml` — FAIL listant les modèles réellement exposés + hint `scripts/setup_opencode.py` (qui régénère `provider.local` selon le contrat d'alias générique). Branché dans le préflight par défaut (ni GPU, ni LLM, ni effet de bord). 7 tests (dont la reproduction du décalage de clé) ; validé en live (config réparée → OK, sauvegarde de l'état cassé → FAIL).
- **Qualification du binaire `llama-server` (version réelle / bibliothèques / CUDA) — fin des échecs silencieux au runtime.** L'install ne faisait que *trouver* un binaire (3 chemins en dur, premier exécutable gagné) sans vérifier qu'il **chargerait** nos modèles — trois trous : (1) **version** : les archis `gated-delta`/`gemma4` (profils 12→64 Go) exigent llama.cpp ≥ b9630, mais un binaire trop vieux échoue *silencieusement* (« unknown model architecture ») ; PIÈGE découvert : le numéro de `--version` est **non fiable** — un binaire **réellement b9632** compilé depuis un clone git se déclare `version: 579` (compteur de build faux). (2) **bibliothèques** : un build maison résout ses `.so` par RPATH (env conda) — env déplacé/supprimé ⇒ `ldd` « not found » ⇒ le serveur ne démarre pas, alors que le fichier est bien exécutable (mode d'échec le plus courant). (3) **recherche étroite** : `/opt`, `~/ik_llama.cpp`, envs conda non trouvés. Nouveau module **pur** `transcria/gpu/llama_runtime.py` (parseurs `--version`/`git describe`/`ldd`, `evaluate_runtime`) : résout la **vraie version via l'arbre git** (source autoritaire) et ne traite le self-report que comme signal *mou*. **`critical` est réservé à ce qui empêche le binaire de DÉMARRER** : seules les **libs manquantes** y donnent (indépendant du modèle — RPATH/conda cassé). **Toute version trop basse n'est qu'un avertissement** — confirmée par git *ou* self-report — car le besoin ≥ b9630 est **relatif au modèle chargé** (les archis gated-delta/gemma4 l'exigent ; un autre modèle tourne sur une version plus vieille) et de toute façon **rien n'est bloquant** (l'install signale mais propose toujours le binaire ; le check ne fait qu'afficher). Build CPU-only ⇒ avertissement. Un fork au schéma de versions distinct (ik_llama : tags `tNNNN`, pas de `bNNNN`) n'est jamais pénalisé pour l'absence de `bNNNN`. CLI `scripts/detect_llama_server.py` (recherche élargie, sorties human/shell/json, codes 0/2/1) : branché dans `install.sh` (repli défensif sur l'ancienne boucle) et ajouté en fin de `scripts/check_arbitrage_llm.sh`. Escape-hatch `LLAMA_LD_LIBRARY_PATH` honoré par les 6 profils (`scripts/arbitrage_profiles/`) pour un env de libs non standard. Logique **entièrement testée sans binaire ni GPU** (`tests/test_llama_runtime.py`, dont le piège b9632→579 et la primauté de `git describe`).
- **Planification du placement LLM par carte (fin du choix « à la somme ») + contrôle de calibration mesuré.** L'install choisissait le palier sur la VRAM **totale** (`recommend_llm_tier(GPU_VRAM_TOTAL_MB)`) puis appliquait un profil au placement **figé** (mono-GPU pour 12/16/24, split fixe `[0,1]`/`[0,1,2]` pour 32/48/64) — ce qui OOM sur les topologies courantes : *2× 8 Go* (palier 16 mono sur une carte de 8), *cartes hétérogènes* (8+24 → split égal 15,8 Go sur la carte de 8), *1× 5090* (palier 32 splité sur une seule carte), ou *2× 5090* (palier 64 écrit `[0,1,2]` pour 2 cartes). Nouveau module **pur** `transcria/gpu/llm_placement.py` : table d'**empreintes mesurées** par palier (poids + KV au contexte + compute, cf. `docs/BENCH_LLM_PALIERS.md`) et logique qui choisit **le plus grand palier réellement plaçable** sur la topologie réelle (vérifie chaque carte, mono ou split égal, marge plancher 1,5 Go calée sur le bench), **jamais un placement qui OOM** ; sinon « transcription brute » avec un **indice actionnable** (« vos 2 cartes hébergeraient le modèle 16 en split, profil --tensor-split personnalisé requis »). `install.sh` s'appuie dessus (repli défensif sur la table par somme si le planner échoue) et écrit la **vraie calibration par carte** (`llm_vram_mb` / `llm_gpu_indices` / `llm_vram_mb_per_gpu`) via `transcria/config/gpu_calibration.py` (round-trip **ruamel**, atomique, préserve commentaires/secrets — là où le `sed` de `switch_arbitrage_llm.sh` échouait silencieusement sur les listes YAML par blocs). CLI `scripts/plan_llm_placement.py` (`plan` / `verify`) : `verify` **mesure** la VRAM réellement consommée par carte (nvidia-smi, ancrage UUID) et la compare au déclaré → signale dérive (calibration périmée), marge critique (OOM imminent, ex. quant Q4_K_M @0,2 Go), carte déclarée inutilisée, débordement hors placement ; greffé en fin de `scripts/check_arbitrage_llm.sh` (au passage : bug `max_tokens: 5` corrigé — un modèle *reasoning* dépense ses premiers tokens dans `<think>`, `text` restait vide et concluait à tort à une panne). Logique **entièrement testée sans GPU** : `tests/test_llm_placement.py` rejoue tout l'univers des cartes NVIDIA (mono / homogène / hétérogène / archis anciennes) + l'évaluateur de calibration ; `tests/test_gpu_calibration_writer.py` garantit le round-trip non destructif (98 % de couverture sur les deux modules).
- **Bench Phase B (vocabulaire technique / temps / VRAM) + palier 24 Go re-tranché.** Nouvel axe de bench sur un **audio technique local (~17 min)** mesurant la **fidélité des noms propres déformés par la STT**, le **temps de traitement** et la **VRAM réelle** — en plus de la qualité. Résultats clés (cf. `docs/BENCH_LLM_PALIERS.md` § Phase B) : (1) **le temps n'est pas monotone avec la taille** — un dense 12B (Gemma) est **5× plus lent** qu'un MoE 35B-A3B ou un hybride SSM 9B ; (2) le **score qualité (100/100) est structurel et aveugle à la fidélité sémantique** → lecture humaine indispensable ; (3) le KV du 35B (Gated Delta Net) est négligeable → budget VRAM dicté par les poids. **Palier 24 Go : Gemma 4 12B écarté** (5× plus lent, régressions de correction, rapport en anglais) → remplacé par **Qwen3.6-35B-A3B UD-IQ4_NL_XL** (profil `24gb_qwen3.6-35b-a3b-iq4nl-xl.sh`) : même modèle de référence en 4-bit i-quant, qualité de référence sur **1 carte 24 Go** (~19 Go @256K). 4 quants testés (Q3_K_XL écarté = spirale agentique ; Q4_K_M écarté prod = OOM @256K) ; aucun quant ne compense la latence du 35B mono-GPU (~2,5× le Q6 réparti sur 2 GPU). `install.sh` (table palier 24) mis à jour.
- **Sélection automatique de la LLM d'arbitrage selon la VRAM (install.sh) + profils par palier portables + bench Phase A.** `install.sh` détecte la VRAM GPU totale, **recommande le palier** (12/16/24/32/48/64 Go), propose de **télécharger le GGUF adapté** (client `hf`, repli `huggingface-cli` ; table repo/fichier vérifiée chez `unsloth`) puis **bascule** dessus via `switch_arbitrage_llm.sh` ; < 12 Go → transcription brute annoncée. Les 6 profils `scripts/arbitrage_profiles/<palier>_*.sh` sont rendus **portables** (`${MODELS_DIR:-…}`, `${LLAMA_SERVER:-…}` — l'install pose les défauts machine). **Modèles retenus (validés par lecture humaine, cf. `docs/BENCH_LLM_PALIERS.md`)** : 12 Go Qwen3.5-9B Q5_K_M (192K), 16 Go Qwen3.5-9B Q6_K, 24 Go Gemma 4 12B Q6_K, 32 Go Qwen3.6-27B Q5_K_M (192K), 48/64 Go Qwen3.6-35B-A3B (Q6/Q8, référence). Bench Phase A (test2.mp3) : le LFM2.5-8B (12 Go) échoue le workflow agentique → **remplacé** par Qwen3.5-9B Q5 ; le Gemma 26B-A4B **Q4_K_M** émet des artefacts (glyphes/JSON cassés) → **remplacé** par Qwen3.6-27B Q5 (niveau référence) ; Q6 ≈ Q8 sur le 35B. Contextes 12/32 Go calés à **192K** (mesuré : ≥1 Go VRAM libre). Au passage : `CUDA_HOME` des profils aligné sur la CUDA réelle (13.1 ; le binaire embarque un RPATH vers ses libs).

### Changed
- **Prompts LLM v3.0 (les trois) : refonte rédactionnelle sans perte de règles — le cœur du pipeline préparé au multi-LLM** (sauvegardes v2 : `configs/prompts/backup_v2/`). Constat de l'audit : la hiérarchie des valeurs était implicite dans le « volume sonore » (majuscules, répétitions), qui récompensait la procédure plutôt que la fidélité — un modèle fort (Qwen) reconstruit la hiérarchie, un modèle moyen suit le volume (vérifié : Ministral a respecté les règles bruyantes et violé le verbatim, règle discrète). v3 : (1) **contrat de priorités en tête de chaque prompt** (« en cas de conflit, la règle la plus haute gagne ») — fidélité verbatim / zéro invention deviennent les règles les plus saillantes, les majuscules leur sont réservées ; (2) **consigne subagent citable** (bloc à recopier dans le prompt du subagent @general — qui est un appel de prompt opencode) : fin de l'ambiguïté du « tu » qui basculait entre agent principal et subagents ; (3) **renumérotation propre** (fin des §4bis/§8b/étape 4b — cicatrices d'accrétion) ; (4) gabarits de sortie **inchangés au caractère près** (alignés sur les parseurs réels : `**Titre suggéré :**` deux-points dans le gras, `SPEAKER_XX [label] : rôle`, séparateurs `|`/`;`/`||`, bloc JSON 8 champs — inventaire complet du code effectué avant réécriture) ; (5) exigence « haute qualité » conservée partout et opérationnalisée quand possible (6-12 paragraphes pour 1-2 h). AUCUNE règle v2 supprimée (balayage anti-perte de ~100 règles-clés exécuté) ; mécanismes éprouvés intacts (délégation @general, blocs 80/100, grep de re-ancrage, cumulatif, stagnation, ratio). Relecture finale : contenu v1.0 conservé quasi tel quel + contrat. 3 tests d'intégrité réécrits sur l'intention (ils épinglaient les numéros de section fragiles). À valider en réel sur test2.mp3 avec Qwen avant tout test de palier.

### Fixed
- **Admission distante (mode serveur de ressources GPU) : plus de famine quand le nœud n'énumère pas ses GPU + check d'installation** — `remote_vram_admits` retournait `False` (et le scheduler `_resources_available` diffère le dispatch sur `False`) quand `/capabilities` ne listait **aucun** GPU exploitable (`gpus` vide, ou `free_mb` illisibles partout — ex. `nvidia-smi` KO sur le nœud) → **jobs différés en permanence**, alors que le contrat dit « données insuffisantes → `None` (laisser le pré-vol décider, l'autorité) ». Corrigé : `None` quand aucune donnée GPU exploitable, `False` seulement quand on a vu des GPU et qu'aucun ne tient (cas normal inchangé). Trois renforts : (1) le fix ci-dessus ; (2) **log throttlé** côté scheduler (`remote_gpu_data_missing`, prédicat pur) — un `WARNING` émis **une fois au changement d'état** « nœud joignable mais sans donnée GPU » (pas à chaque tick) ; (3) **check d'installation** `doctor.check_inference_node_gpus` : un nœud distant joignable qui n'énumère aucun GPU via `/capabilities` → `WARN` clair (détecté à l'install, pas via des jobs qui stagnent en prod). Pas d'alerte admin runtime dédiée (l'impact réel — job bloqué — est déjà couvert par `alert_admin_vram_wait` / le mode différé §7.2). N'affecte pas les modes all-in-one / frontale (où `remote_requirements` est vide → `None` déjà). 9 tests (`test_resource_status` : `None` sur gpus vide / free_mb illisibles, `False` si présents mais aucun ne tient, prédicat `remote_gpu_data_missing` ; `test_doctor` : WARN/OK/skip du check d'install).
- **Estimateur de concurrence STT (outil offline) : div-zéro sur mesure dégénérée** — `stt_concurrency_estimator.estimate_local_concurrency` recomputait `estimated_speedup` par `baseline / estimated_transcribe_s` → `0/0` si `transcribe_s == 0` (audio dégénéré). Remplacé par la valeur déjà calculée (`_speedup()`, ≥ 1.0), identique pour `baseline > 0`. Outil **offline** (non importé par le runtime) → aucun impact sur les modes. 1 test. (Revue du périmètre « perf » : `workflow/concurrency_profile.py` jugé sain — observabilité best-effort, divisions gardées, `transcribe` dérivé sériel-local/délégué-distant correctement par mode.)
- **Rapport DOCX : deux vecteurs de crash supprimés (le livrable final ne se perd plus sur une donnée mal typée) — revue du sous-système** — sous-système jugé bien gardé par ailleurs (coercition `_as_str_items`, `int()` quorum sous try/except, `prochaine_date` gardé, `_section_type_specific` coercé, appelants attrapant l'échec). Deux durcissements : **(1)** `_section_context` faisait `ctx.get("topic", "").strip()` — or une clé présente à `null` (LLM sans suggestion / champ vidé) renvoie `None` (le défaut de `.get` ne couvre que les clés **absentes**) → `None.strip()` plantait **tout** le rapport. Corrigé en `str(ctx.get("topic") or "").strip()` (idem `objective`/`notes`) — gère null/absent/vide/non-chaîne. **(2)** `DocxReport.__init__` n'gardait pas `structured_data` par `isinstance` (contrairement à `participants`) → un non-dict (legacy/malformé) plantait `_section_enriched` (`sd.get` sur un non-dict). Corrigé. Rappel : la génération reste **tout-ou-rien** (un crash perd le document entier ; les appelants l'attrapent → DOCX absent du ZIP + WARNING, ou 500 à la demande) — la résilience par section reste un chantier séparé. 2 tests (contexte à `null`, structured_data non-dict → DOCX généré sans crash).
- **Web/auth : durcissement sécurité (cookie SameSite/Secure, open redirect, logout POST) — revue du sous-système** — points sains confirmés (hachage werkzeug, autorisation `@requires(Permission)`, IDOR job via `_can_access_job`, pas de `shell=True`, pas de path-traversal `job_id`). Quatre durcissements (audit) : **(1) anti-CSRF sans jeton** : `SESSION_COOKIE_SAMESITE="Lax"` (+ `HTTPONLY=True` explicite) — le cookie de session n'est plus envoyé sur un POST cross-site, ce qui neutralise le CSRF sur les routes mutantes (création d'admin, suppression de job, sauvegarde de config) que les décorateurs `@requires` ne protégeaient pas (le CSRF emprunte la session autorisée de la victime). Le `fetch` same-origin du wizard et les échanges inter-tiers (frontale↔scheduler↔GPU, via DB/API machine-à-machine sans cookie) ne sont pas affectés. **(2) open redirect** : le login validait `next.startswith("/")`, qui laissait passer `//evil.com` (protocol-relative) et le contournement par `\t\r\n` — remplacé par `_is_safe_next_url` (rejette protocol-relative, schéma, netloc, et neutralise les caractères de contrôle comme les navigateurs). **(3) `SESSION_COOKIE_SECURE`** piloté par `security.session_cookie_secure` (défaut False pour ne pas casser le HTTP dev/all-in-one/GPU interne ; **à activer derrière HTTPS**). **(4) logout en POST** (était GET → un `<img src="/logout">` déconnectait en CSRF) ; lien nav converti en formulaire POST. Restent flagués comme chantiers séparés (plus invasifs) : jetons CSRF complets (défense en profondeur) et throttling de login. Tests : `_is_safe_next_url` (local OK / protocol-relative / schéma / bypass \t\r\n / vide rejetés), flags cookie appliqués, logout POST OK + GET → 405.
- **Stockage/SQL : race *lost-update* sur `extra_data` corrigée (verrou de ligne) + WAV intermédiaires exclus de la synchro pg (revue du sous-système)** — push/pull jugés sains (transaction par fichier, sha256 vérifié, matérialisation atomique, conflits non écrasés, mtime non préservé → cohérent avec le content-sha de la reprise). Deux correctifs : **(1)** `JobStore.update_extra_data` faisait un read-modify-write **sans `FOR UPDATE`** ; or `extra_data_json` est partagé entre tiers/threads (frontale : annulation/contexte ; scheduler : progression toutes les quelques secondes, marqueurs de reprise). Deux écritures concurrentes du même job se perdaient — scénario réel : une **demande d'annulation écrasée** par une mise à jour de progression (l'updater `{**extra}` réécrit tout sur une lecture périmée) → le job continue malgré l'annulation. Vaut en all-in-one (deux threads, sessions distinctes) et en pg multi-tiers (deux process). Correctif : `with_for_update()` + `populate_existing` sérialise et relit la valeur committée sous le verrou (motif éprouvé de `QueueStore.claim()`). FOR UPDATE est émis en **PostgreSQL** (backend de prod/concurrence) ; ignoré sans erreur en SQLite (mono-process, verrou d'écriture global). **(2)** Les WAV dérivés du préprocess (`input/vocals.wav`, `scene_filtered.wav`, `denoised.wav`, `normalized.wav`) étaient sous `input/` (synchronisé) malgré l'intention « intermédiaires locaux au worker » → en backend pg avec prétraitement activé, ces gros fichiers **recalculables** gonflaient la base. Désormais exclus de la synchro (`_EXCLUDED_AUDIO_INTERMEDIATES`) ; seul `input/original.{ext}` voyage (un upload `.wav` reste donc synchronisé). La reprise n'en dépend pas (préprocess rejoué sur un autre worker, déjà géré). 7 tests (`test_job_store` : None, copie de l'updater, accumulation séquentielle annulation+progression ; `test_artifact_store` : intermédiaires non poussés, `original.wav` toujours poussé).
- **Queue/allocator : filet de sécurité VRAM en fin de job + exceptions de thread surfacées (durcissement, revue du sous-système)** — la revue logique de `queue/allocator` n'a révélé **aucun bug actif** (claim atomique `FOR UPDATE SKIP LOCKED`, scheduler unique par verrou consultatif PG gating le dispatch, allocateur en mémoire cohérent car un seul process dispatche, libérations par phase via `GPUSession.__exit__`/`finally`). Deux durcissements défensifs, utiles pour un process unique à longue vie gérant une ressource rare : (1) `QueueScheduler._on_done` réclame désormais toute **réservation VRAM résiduelle** du job via le nouvel `GPUAllocator.release_reservations(job_id)` — accounting uniquement, par job, idempotent, **sans toucher au verrou LLM** (`threading.Lock` partagé, dont la libération hors propriétaire est délicate — déjà gérée par les `finally` du pipeline) ; si ce filet récupère réellement des Mo, il loggue un `WARNING` (une phase n'a pas libéré = à investiguer). `release()` = `release_reservations()` + `release_llm()` (comportement inchangé). (2) `_on_done` inspecte `future.exception()` et loggue toute exception non gérée du thread de job (jusqu'ici avalée par le `Future` — `_run_process` gère ses erreurs mais une défaillance dans son `except`/`finally` disparaissait sans trace). 6 tests (`test_gpu_allocator` : release_reservations cible/idempotence/n'touche-pas-le-verrou-LLM, régression `release()` ; `test_queue_scheduler` : exception surfacée + filet déclenché).
- **Reprise : un skip transitoire de la relecture finale n'est plus gravé « fait » (fin d'une perte silencieuse)** — revue logique du workflow/reprise. `run_final_review` est best-effort : si la LLM est momentanément indisponible (occupée par un autre job, VRAM insuffisante, non prête), elle retournait `{success: True, skipped: True}`. Or la boucle pipeline checkpointe tout résultat non-échoué → la relecture était **marquée faite** sur un skip dû à une **contention passagère**, donc **jamais rejouée** : sous charge multi-utilisateur, un job se terminait `COMPLETED` avec synthèse non harmonisée, variantes de glossaire non unifiées et audit des données structurées non fait — silencieusement (seul signal : un WARNING). Correctif : les skips transitoires sont marqués `retryable=True` (`llm_busy`/`vram_insufficient`/`llm_unavailable`) et le pipeline les enregistre via `resume.mark_phase_skipped` (→ `extra_data.pipeline.skipped_phases`, auditable) **sans** les marquer faits → rejoués à un re-traitement ; les skips **permanents** (`enabled=false`/`no_corrected_srt`/`nothing_to_review`) restent légitimement marqués faits. `mark_phase_done` nettoie le flag skipped quand la phase finit par réussir. Provenance cohérente : une relecture rejouée réécrit le SRT corrigé → `quality`/`export` se ré-exécutent. 6 tests (`TestProvenance` : skip transitoire non gravé / skip permanent gravé ; `TestSkippedPhases` : enregistrement, retrait d'un marqueur périmé, nettoyage au succès). Cf. `docs/PIPELINE_REPRISE.md` §4.
- **Chasse aux bugs (passe outillage + motifs) : garde-fou `agent_work_dir` + 3 cosmétiques** — `resolve_agent_work_root` résout l'override en **absolu** (un chemin relatif tombait sous le dépôt → réintroduisait la pollution `AGENTS.md`) et loggue un WARNING si un `AGENTS.md`/`CLAUDE.md` est ancêtre du scratch. + B033 (doublon `"Ça"`), B904 (`raise … from None`). Revue (fuites de fd, `except` nus, asserts sous `-O`, divisions par zéro, chaînes `.get`) : aucun autre bug. (Note : le `getattr(transformers, …)` de `_cohere_tf5_worker.py` est **volontairement conservé** — l'accès attribut direct casse mypy `attr-defined`, la classe n'étant pas dans les stubs.)
- **Agents LLM : scratch déplacé HORS de l'arbre du dépôt — fin de la pollution `AGENTS.md` et du déraillement de la relecture finale** (incident job 6f4f4cad, 13/06/2026). Forensique de la session opencode : la relecture finale ne produisait que **2 fichiers sur 4** (synthèse harmonisée + SRT relu, mais ni l'audit des données structurées ni le rapport) — d'où les variantes de glossaire non unifiées et l'incohérence PEI/PUI dans le DOCX final, le tout rapporté en **« succès » silencieux**. Triple cause, une seule racine : le scratch vivait sous `jobs/<id>/work/`, **dans l'arbre git**, or opencode fixe sa racine de projet en **remontant** depuis le cwd. (A) Il chargeait donc le `AGENTS.md` du dépôt (~95 Ko de doc dev) dans le contexte de **chaque** agent étroit (résumé, correction, relecture). (B) Il ancrait `bash`/`read`/`write` sur la racine git, pas sur le scratch → les chemins relatifs de l'agent échouaient (`FileNotFoundError` répétés). (C) Acculé, l'agent a tenté d'écrire un script dans `/tmp` → permission `external_directory` **rejetée en mode headless** (pas de TTY pour répondre) → run avorté avant l'audit structuré et le rapport. **Correctif** : le scratch est désormais `<storage.agent_work_dir>/<job_id>/<phase>/` **hors du dépôt** (défaut `<tempdir>/transcria-agent-work/`, override config) → opencode prend le scratch comme racine de projet (contexte propre, chemins relatifs fiables, tout in-project) ; `TMPDIR` pointé sur le scratch (temporaires réflexes in-project) ; `AgentWorkspace.purge_job()` nettoie à la suppression du job (le scratch n'est plus sous `job_dir`). `AGENTS.md` **ne bouge pas** (les agents de codage le gardent) — seul le lieu d'exécution des phases change. **Observabilité** ajoutée (« inadmissible de ne pas l'avoir vu ») : la relecture loggue un `WARNING` si <4 fichiers produits (avec les manquants nommés) ou si opencode sort non-zéro — la livraison partielle n'est plus invisible. Vérifié sans impact : UI, export/DOCX/ZIP, file de jobs, synchro `pg`, inférence distante, validation/UI de config (`_deep_merge` préserve la clé), audit, SQL, recréation de fichiers. 11 tests (`test_agent_workspace.py` : scratch hors dépôt + aucun `AGENTS.md` ancêtre, `resolve_agent_work_root`, `purge_job` ; `test_opencode_runner.py` : `TMPDIR`, WARNING 2/4 ; `test_job_service.py` : purge à la suppression). Cf. `docs/PIPELINE_REPRISE.md` §10.3. Hors périmètre (suite) : non-délégation `@general` + `str.replace` brut de l'agent, à réévaluer après validation E2E de ce correctif.
- **Prompts LLM (correction v2.3, résumé v2.9) : retouches chirurgicales post-audit + contrats verrouillés par la CI** — analyse des 3 prompts (résultat de très nombreux tests : aucun mécanisme nouveau, uniquement le factuellement faux et l'implicite devenu dangereux). (1) **Urgent** : la vérification de taille de la correction utilisait `../metadata/transcription.srt` — chemin mort depuis l'isolation `AgentWorkspace` (l'agent tourne dans un scratch) → comparaison avec la copie locale `transcription.srt`. (2) **Incohérences flagrantes des greps de re-ancrage** : le pattern du résumé se terminait par une **alternative vide** (`\|"`) qui matchait TOUTES les lignes, et les deux prompts affirmaient viser « la zone du bloc courant » alors que `head -40` renvoie toujours le début du fichier → pattern réparé, sortie bornée, consigne honnête (filtrer par numéros de ligne à SA plage) — la fonction de rituel de ré-ancrage est préservée. (3) **Portabilité descendante** (multi-LLM, Qwen prioritaire — qui satisfait déjà tout) : préfixe `SPEAKER_XX(Nom):` déclaré explicitement intouchable dans la correction (la formulation abstraite a suffi à Qwen mais pas à un 14B, qui réécrivait en `Nom:`) ; avertissement « summary.md est PARSÉ par le code » avant le gabarit (sections nommées, séparateurs, JSON). (4) **Garde code** `_corrected_srt_integrity_error` : parité du nombre de segments (toujours) + ratio 0.90–1.10 (au-delà d'une taille minimale) à la collecte de la correction — un SRT tronqué/réécrit ne passe plus avec « non vide » ; échec explicite relançable. (5) **Tests de contrat prompt↔code** (`tests/test_prompts_contract.py`) : 18 types de réunion, sections parsées, champs structured_data, fichiers de sortie, plus jamais de `../` ni d'alternative vide. 16 tests. Relecture finale (v1.0) volontairement intouchée.
- **Rapport DOCX (produit final, 18 types de réunion) : passe qualité complète** — audit demandé sur le livrable final. (1) **Colonne Locuteur perdue** : le parseur SRT n'acceptait que `SPEAKER_XX(Nom):` ; or le SRT corrigé peut arriver au format lisible `Nom: texte` (observé en réel : un agent LLM réécrit le préfixe malgré la consigne) → la colonne restait vide et le nom collé au texte ; repli prudent `Nom: texte` ajouté (majuscule initiale accents inclus, ≤ 40 car.). (2) **Crash possible de la génération** : `_apply_final_review` stockait le JSON relu **brut** — un item non-chaîne (dict/scalaire) faisait planter `add_run()` → normalisation canonique « listes de chaînes » à l'application (réutilise `_normalize_structured_data`) + coercition défensive côté DOCX. (3) **« Points à vérifier » ne montrait que 3 checks sur ~15** : il cachait notamment `speaker_name_violations` (nom de locuteur ALTÉRÉ par la LLM — severity error), `missing_lexicon_terms`, `unmapped_speakers`, `foreign_segments`, `non_latin_segments` → rendus dédiés + **repli générique** (aucun avertissement qualité n'est caché au lecteur du document final). (4) **Le résumé (`summary.md`) et `final_review_report.md` manquaient au ZIP final**. (5) Page de garde : **durée de la réunion** affichée (dernier timestamp du SRT) et **pied de page supprimé de la couverture** (plus de « Page 1/N » sur la garde) ; gras markdown de la LLM rendu en vrai gras dans les sections décisions/actions (plus de `**` littéraux) ; double `@staticmethod`, annotation `-> int`, `p.get("id")` défensif. 18 tests (parseur nom simple, durée, structured_data robuste, couverture des checks, ZIP, normalisation relecture).
- **Session perdue = « Réponse serveur invalide » + martèlement silencieux** (incident du 12/06/2026, bouton Résumé) : quand la session du navigateur devenait invalide (observé : cookie écrasé/disparu entre deux polls), toutes les routes `/api/` répondaient **302 → page HTML de login** ; `fetch` suivait la redirection (status 200 + HTML), le front échouait à parser le JSON (« Réponse serveur invalide. ») et **continuait de poller non authentifié pendant des heures** sans signal exploitable. Trois corrections : (1) `unauthorized_handler` Flask-Login — une route `/api/` non authentifiée répond désormais **401 JSON** (`auth_required: true`), les pages HTML gardent la redirection historique ; (2) `wizard-api.js` détecte 401 (ou une redirection résiduelle vers `/login`) et **renvoie l'utilisateur se connecter** (`/login?next=…`) au lieu d'afficher une erreur cryptique et de boucler ; (3) cause racine la plus probable de la perte de session neutralisée — **cookie de session nommé `transcria_session`** : les cookies ignorent le port, donc sur un hôte qui sert plusieurs apps web en `127.0.0.1` (machine dev/all-in-one typique), une autre app Flask posant le cookie par défaut `session` écrasait le nôtre et déconnectait TranscrIA en silence (les sessions en cours au déploiement devront se reconnecter une fois). 4 tests (`TestUnauthenticatedResponses`).
- **Pipeline reprenable v2 : provenance des artefacts (plus jamais de skip aval périmé)** — trou d'architecture révélé par le job réel 4bda98cb : la v1 décidait du skip **localement à la phase** (« marqueur ou artefact présent ») alors que le pipeline est une **chaîne de dépendances** ; la correction rejouée produisait un nouveau SRT corrigé mais `quality` voyait son `quality_report.json` présent → **skip** → rapport 97/100 affiché alors qu'il avait été calculé sur le SRT **brut** d'un run précédent, sans aucun signal. Désormais chaque phase **déclare ses entrées** (`resume._PHASE_INPUTS`) ; le checkpoint enregistre leurs **empreintes sha256** (`extra_data.pipeline.phase_inputs`) ; le skip exige marqueur + artefact déclaré + **empreintes inchangées** — l'invalidation aval est une *conséquence* (amont rejoué → empreinte ≠ → ré-exécution), pas un bookkeeping d'ordre, et un amont rejoué à sortie byte-identique laisse l'aval légitimement sauté. Au mismatch, le marqueur est **retiré en base avant d'exécuter** (`unmark_phase`) : l'admission VRAM et l'UI restent vraies même si un `vram_wait` coupe la chaîne. Choix assumés (tous topologie-sûrs) : fraîcheur par **contenu, jamais par mtime** (le pull `pg` ne préserve pas les mtimes — toute solution mtime serait cassée cross-machine) ; **audio exclu des empreintes** (débruitage non bit-exact entre machines : l'empreinter ressusciterait la boucle de re-STT) ; **doute → re-run** (marqueur sans empreintes = legacy = rejouer) ; rétro-remplissage « artefact ⇒ fait » restreint à `transcription`. 6 tests de provenance (`TestProvenance` : scénario du bug, idempotence byte-identique, legacy, mtime-insensible, artefact orphelin, unmark persisté). Cf. `docs/PIPELINE_REPRISE.md` §10.
- **Isolation des agents LLM : un agent ne peut plus corrompre un artefact canonique** — incident sur le même job : l'agent opencode de correction (cwd=`metadata/`, outil Edit actif) a **réécrit `transcription.srt`**, l'artefact SOURCE du job (le brut n'a survécu que dans `transcription_segments.json`) — or tout le modèle « l'artefact fait foi » suppose les artefacts immuables une fois checkpointés. Nouveau module `transcria/workflow/agent_workspace.py` : chaque phase agent (correction, relecture finale, résumé) tourne dans un **scratch** `jobs/<id>/work/<phase>/` (hors `SYNCED_PREFIXES` : jamais synchronisé) avec des **copies** de ses entrées ; le runner collecte les sorties du scratch et écrit lui-même le canonique (atomique, validations retry ≤3 / ratio 0.9–1.1 inchangées) ; après l'agent, `verify_and_restore_sources()` re-hash les canoniques — fichier stagé muté → **restauré depuis la copie pristine** (+ ERROR), canonique surveillé (`metadata/`, `context/`, `summary/`) altéré hors stage → signalé. Le matériel de prompt transitoire de la relecture finale (`summary_to_harmonize.md`, glossaire, `structured_data.json`) vit désormais dans le scratch, plus dans `metadata/`. Scratch supprimé après succès, conservé pour diagnostic après échec. 12 tests (`tests/test_agent_workspace.py`).
- **Correction LLM muette = étape validée en silence** (famille e62295c1, observé avec Ministral 14B le 12/06/2026) : quand opencode « réussissait » (exit 0) **sans rien produire** (0 texte, aucun fichier), `run_correction` validait l'étape — SRT **brut** servi comme corrigé, relecture finale sautée (« SRT corrigé absent »), **qualité 97/100 calculée sur du non-corrigé**, pipeline « OK ». Même doctrine que le résumé : **retry ≤ 3** (LLM déjà chargée, seule la passe LLM est rejouée) puis **échec explicite relançable** — le pipeline reprenable ne rejouera que la correction. Au passage, deux classes de test homonymes (`TestWorkflowRunnerRunCorrection`) se masquaient : ~7 tests de correction n'étaient **jamais collectés** par pytest — classe renommée, tests ressuscités et mis au niveau (neutralisation de la réservation VRAM réelle).
- **La sonde de santé de la LLM redémarrait un serveur sain (modèles « reasoning »)** : `ensure_arbitrage_llm_ready` demandait 5 tokens en complétion brute et exigeait du **texte** — or un modèle reasoning (Ministral-3, Qwen3 selon template) dépense ses premiers tokens dans `<think>`, que llama.cpp sépare en `reasoning_content` → `text` vide → serveur **vivant jugé malade** → arrêt + relance à chaque vérification. Désormais : 64 tokens, et la preuve de vie accepte `text` **ou** `reasoning_content`. Corollaire corrigé : le **log de lancement** (`/tmp/arbitrage_llm_<port>.log`) pouvait être inouvrable (`fs.protected_regular` : fichier créé par un autre utilisateur dans /tmp sticky) → sortie jetée → un lancement en échec (exit≠0) était **indiagnosticable** ; repli automatique sur un chemin par utilisateur (`.<uid>`), tracé dans les logs. 4 tests (sonde reasoning/texte/vide, repli du log).
- **Gestion VRAM : passe hétérogénéité (cartes de 2 à 64 Go, pas seulement des 24 Go)** — suite de l'audit VRAM. Trois angles morts corrigés/documentés : (1) la réservation multi-GPU de la LLM divisait **à parts égales** — faux pour des cartes mixtes ou un `--tensor-split` inégal → nouveau réglage `gpu.llm_vram_mb_per_gpu` (part réelle **par carte**, liste alignée sur `llm_gpu_indices`, ex. `[18000, 6000]` pour 24+8 Go ; absent = parts égales, validé par le schéma) ; (2) la sélection de GPU des petites phases (STT/diarisation) ignorait le placement LLM — une phase de 6 Go pouvait se poser sur une carte de la LLM et **bloquer sa relance** alors que d'autres cartes convenaient → les cartes **hors placement LLM sont préférées** à disponibilité suffisante (aucun effet avec le défaut « tous les GPU » ; pas de famine : les cartes LLM restent utilisables si elles seules conviennent) ; (3) `gpu.min_free_vram_mb` (4000) documenté comme **à réduire sur les petites cartes** (4-8 Go). Confirmé sain par l'audit : aucune taille codée en dur, la mesure réelle par carte est l'autorité, le nœud de ressources déclare `gpu_mem` par moteur. 4 tests hétérogènes (split inégal, placement, anti-famine).
- **Gestion VRAM de la LLM d'arbitrage : le modèle mono-GPU était insatisfaisable par construction** (audit déclenché par le test Ministral — merci à l'œil de l'utilisateur : « jamais 60000 ne tient dans une RTX 3090 »). La LLM s'étale sur plusieurs cartes via son script (`--tensor-split`), mais l'allocateur la réservait sur **un seul** GPU : (1) `try_reserve(60000)` ne pouvait **jamais** réussir sur des cartes de 24 Go → code mort tant que la LLM tournait, et **deadlock `vram_wait` garanti** dès qu'il fallait la relancer (après le reclaim d'inactivité, un crash, ou en lancement à la demande) ; (2) le drapeau `llm_shared` du profil était **inconditionnellement vrai** → l'admission ne vérifiait jamais la LLM, même éteinte ; (3) la comptabilité ne correspondait pas au placement réel (choisi par le script, pas par l'allocateur). Nouveau modèle : `gpu.llm_vram_mb` = empreinte **totale** + `gpu.llm_gpu_indices` = cartes du script (défaut tous les GPU) ; `GPUAllocator.can_host_llm`/`try_reserve_llm` vérifient/réservent **par carte** (total ÷ nb, tout-ou-rien, libération en bloc) ; les phases `summary_llm` et `llm_arbitration` du runner utilisent la réservation multi-GPU ; l'admission (`_llm_admissible`) s'appuie sur la **vérité vivante** (LLM en marche → partagée ; éteinte → `can_host_llm` exigé) et le max mono-GPU n'inclut plus jamais la LLM. 14 tests (allocateur multi-GPU, tout-ou-rien, admission par état réel, preuve de l'ancien bug).
- **Résumé « réussi » avec placeholder quand la LLM est indisponible** (découvert en testant le palier Ministral 14B) : la sous-étape LLM du résumé avait conservé **trois skips silencieux** d'avant la doctrine vram_wait — pénurie VRAM, verrou LLM occupé, échec de lancement — qui concluaient `SUMMARY_DONE` avec le **placeholder** (« LLM non configurée ») sans signal. Invisible en prod (la LLM tournait en permanence), systématique dès que la LLM doit être lancée à la demande. Désormais : pénurie VRAM et verrou occupé → **`vram_wait`** (état pré-résumé restauré, mise en attente + reprise auto, STT/diarisation réutilisés du cache) ; échec de lancement → **`summary_llm_failed`** (signalé + relançable, famille e62295c1). Plus aucun chemin ne valide un résumé que la LLM n'a pas écrit. 3 tests de régression.
- **500 à la suppression d'un job déjà traité** (incident du 11/06/2026, `POST /jobs/<id>/delete`) : la relation ORM `Job↔JobQueueEntry` n'avait **pas de cascade** — à la suppression du job, SQLAlchemy « désassociait » l'entrée de file (`UPDATE job_queue SET job_id=NULL`) → violation NOT NULL → Internal Server Error. Tout job **passé en file** (c.-à-d. tout job réellement traité) était insupprimable depuis l'UI. Même classe de bug corrigée sur `voice_matches.job_id` (suggestions de matching vocal → violation FK). Correctif : `cascade="all, delete-orphan"` sur les deux relations — supprimer un job supprime son entrée de file et ses suggestions vocales (le référentiel de voix, lui, survit). Tests de régression : suppression d'un job avec entrée `done`, avec entrée `waiting`, et avec suggestion vocale.

### Changed
- **Vitrine GitHub** : `README.md` réécrit **en anglais** (positionnement self-hosted/souveraineté, différenciateurs, captures d'écran de la nouvelle interface dans `docs/screenshots/`, quickstart, statut honnête « French-first ») ; l'ancien README français est conservé et mis à jour dans `README.fr.md` (liens croisés). **Licence : AGPL-3.0 → Apache-2.0** (permissive avec concession explicite de brevets — l'AGPL freinait l'évaluation/adoption ; le détenteur du copyright a validé le changement). Discipline « prêt-à-traduire » gravée dans AGENTS.md (libellés via `ui_labels.py`, prompts en fichiers, pas de messages d'affichage figés en base) — l'i18n reste une évolution possible sans refonte, pas un chantier actuel.

### Added
- **Refonte de l'interface web (lots A→D, `docs/REFONTE_UI.md`)** : (A) feuille de style dédiée (`static/css/transcria.css`, tokens + composants — fin des styles inline), navbar à **menus déroulants** (« Administration », menu utilisateur) avec page active surlignée, **libellés français centralisés** des états de job (`transcria/web/ui_labels.py`, filtres Jinja `state_label`/`state_badge` — plus jamais d'état brut `ready_to_process` à l'écran) ; (B) **stepper du wizard sans troncature** (libellés complets — fini le `label[:10]` —, largeurs égales, coche/erreur par étape) et **bandeau d'échec** affichant `error_message` avec bouton « Relancer le traitement » (le cas FAILED était muet au rechargement) ; (C) configuration enrichie : onglet **« Prompts LLM »** (édition des 3 prompts `configs/prompts/` — liste fermée côté serveur, garde non-vide + taille max, copie de secours `.bak`, écriture atomique, audit log) et onglet **« Scripts (lecture seule) »** (contenu des scripts configurés, badges prêt/introuvable/non exécutable — édition web refusée par décision de sécurité) ; (D) page **Système consciente du rôle** (badge tout-en-un/frontale, panneaux GPU locaux masqués sur une frontale CPU-only avec encart explicatif, avertissement dashboard adouci) + carte **« Stockage des fichiers de jobs »** (backend `fs`/`pg`, volumétrie `job_files`). Décisions : pas d'explorateur SQL de la base (sécurité, redondant avec File/Audit/metrics).
- **Magasin de fichiers de jobs partagé via PostgreSQL (`storage.shared_backend: pg`)** — ferme le **trou d'architecture** de la topologie split : la frontale (`role=web`) et le worker GPU (`role=scheduler`) partageaient la base mais **pas les fichiers** (`jobs_dir` local à chaque machine). Sur deux machines sans montage commun : audio introuvable côté worker (job `failed` au dispatch), invitation/lexique **silencieusement ignorés** par le worker, téléchargements 404 et DOCX en 500 côté frontale. Plutôt que d'exiger un NFS (infra hors code, intégrité non gérée), les fichiers **suivent le même chemin que l'état du job : la base** — `transcria/jobs/artifact_store.py`, tables `job_files`/`job_file_chunks` (migration `b7c4e1a9f3d2`), chunks de 8 Mo (mémoire bornée jusqu'au plafond d'upload 1 Go), **sha256 vérifié à la matérialisation**, écriture atomique tmp+rename, manifeste local `.sync_state.json` (un fichier local modifié non poussé n'est **jamais** écrasé). Frontale : push à l'upload, à l'enfilage et après toute écriture HTTP réussie (hook global `after_app_request` — les endpoints futurs sont couverts d'office) ; **pull paresseux throttlé** avant les requêtes portant un `job_id` ; package zip **reconstruit localement** à la demande (exclu de la synchro : il contient l'audio). Worker : pull au début de `_run_process` (reprise **portable entre workers** : les artefacts re-matérialisent `is_phase_done`), **push au checkpoint de phase AVANT le marqueur** (une phase n'est « faite » que si ses artefacts sont durables en base ; un push raté → phase rejouée), **purge des blobs `input/`** aux états terminaux du pipeline complet (re-poussés à l'enfilage en cas de reprocess). Le nœud de ressources ne stocke jamais de fichier utilisateur (upload HTTP → fichier temporaire supprimé). Défaut **`fs` inchangé** (tout-en-un / NFS : zéro octet en base). Garde-fous : check `doctor` « Stockage des fichiers de jobs (split) », validation schéma (`pg` ⇒ base PostgreSQL), réglage dans `/admin/config` (« File & exécution »). Durcissement connexe : un `audio_path` de reprise absent du disque local fait **rejouer le préprocess** au lieu d'échouer sur un chemin mort. Conception, garanties et suivi : `docs/STOCKAGE_PARTAGE_JOBS.md`.

### Changed
- **Pipeline de traitement reprenable (checkpoint/resume)** — traite la **cause racine** des blocages de file qu'on corrigeait jusqu'ici phase par phase. Avant, `PipelineService` relançait tout depuis le début à chaque re-dispatch ; un re-queue (`vram_wait`/`deferred`) **refaisait tout le STT**, d'où boucles de re-STT et risque de worker figé. Désormais le pipeline **saute les phases déjà faites** (marqueur persistant `extra_data.pipeline.completed_phases` ∪ artefact atomique sur disque) et **reprend à la première incomplète** (`transcria/workflow/resume.py`). Effets : (1) un re-queue **ne refait plus** le travail accompli ; (2) l'**admission** du scheduler n'exige que la VRAM des **phases restantes** (`_local_required_mb` exclut les phases faites) — un job où il ne reste que la correction exige la VRAM LLM, pas le STT ; (3) `run_correction` revient au contrat `vram_wait` (plus de `FAILED`) : le re-queue **reprend à la correction**, sans boucle ni worker figé — le cas-bord « process tiers occupe la VRAM à la correction » est résolu **par construction**. L'état de reprise est **réinitialisé** à une re-soumission utilisateur (`api_process`) et **préservé** sur les re-queues automatiques. Conception et suivi : `docs/PIPELINE_REPRISE.md`.

### Fixed
- **Détection de locuteurs routée vers le worker GPU sur un frontal CPU-only (split)** : `/api/jobs/<id>/speakers/detect` exécutait pyannote (GPU) en synchrone dans le process web — comme le résumé, impossible sur un frontal `role=web` sans GPU. Désormais, en `role=web`, la détection est **enfilée sur le worker GPU** (mode de file `speakers`, `JobExecutorService.SPEAKER_MODE`) et le client poll `GET /status` ; en `all-in-one`, exécution synchrone inchangée. Généralise le routage des **étapes GPU synchrones** du frontal vers le worker (résumé + détection partagent désormais la même mécanique `STEP_MODES`). Avec ça, **aucune** phase GPU ne s'exécute plus sur le frontal — le nœud de ressources exécute tout ce que le frontal ne peut pas faire.
- **Flake de test `/admin/config`** : la config de test fixait `workflow.queue.poll_interval_s=3600` (pour endormir le scheduler), valeur **invalide** vis-à-vis du schéma (1-300) → le test de sauvegarde échouait selon l'ordre d'exécution. Ramené à `300` (toujours dormant pendant un test, mais schéma-valide) → test déterministe.
- **Résumé routé vers le worker GPU sur un frontal CPU-only (split)** : le résumé synchrone (`/api/jobs/<id>/summary`) s'exécutait dans le process web — impossible sur un frontal **`role=web` sans GPU** (STT cohere + pyannote + LLM 35B sont GPU-bound ; une LLM 35B en CPU est ~100-300× plus lente, inexploitable). Désormais, en `role=web`, `api_summary` **n'exécute pas** le résumé localement : il l'**enfile sur le worker GPU** (mode de file `summary`, comme la reprise serveur) et le client poll `GET /status`. La décision se fait sur le **rôle** (pas une détection matérielle : un éventuel petit GPU frontal est ignoré). En `all-in-one` (web + GPU), exécution synchrone inchangée. Conforme au modèle « le nœud de ressources exécute tout ce que le frontal ne peut pas faire ».
- **Réservation VRAM locale fantôme de la diarisation en topologie split** : `run_diarization` réservait toujours la VRAM locale (`diarization`, défaut 2000 Mo via `_gpu_session`) **même quand `models.diarization_backend=remote`** (la diarisation tourne alors sur le nœud de ressources). Sur un tier sans GPU c'était une fausse contention ; pire, le reclaim pouvait **stopper la LLM d'arbitrage à tort** pour une phase qui s'exécute à distance. `run_diarization` saute désormais le `_gpu_session` local quand `_phase_runs_remotely("diarization")` est vrai — aligné sur le STT du résumé et le STT principal. Audit complet des phases GPU effectué (STT rapide, STT, diarisation, détection locuteurs, LLM arbitrage) : toutes les phases servies à distance sont désormais sans réservation VRAM locale.
- **`transcria doctor --llm-smoke` : échec rapide si la LLM est down** : le smoke lançait opencode même quand le serveur LLM ne répondait pas, attendant le timeout opencode (~120 s) avant d'échouer. Il **pré-sonde** désormais `/v1/models` et, si la LLM est injoignable, renvoie un `FAIL` **immédiat** avec la consigne (« LLM injoignable sur le port N — lancez-la d'abord, `./scripts/launch_arbitrage.sh` ») sans lancer opencode. Sonde injectable (`probe=`) pour les tests.
- **`error_message` obsolète après une reprise réussie** : la colonne `jobs.error_message` n'était jamais effacée — un vieux message (ex. « VRAM insuffisante pour cohere-summary ») restait collé après qu'un job avait repris et progressé (state `ready_to_process`/`completed`), trompant l'UI. Nouvel invariant dans `JobStore.update_state` : **`error_message` non vide ⟺ `state == FAILED`** — toute transition hors FAILED l'efface. Le détail par exécution reste tracé dans `extra_data.execution.last_error` (inchangé).
- **Job bloqué indéfiniment derrière notre propre LLM d'arbitrage chaude (admission)** : la récupération VRAM (`_reclaim_vram_from_idle_arbitrage_llm`) ne se déclenchait qu'**en cours de phase** (post-dispatch). Or l'**admission** du scheduler (`_resources_available` → `can_allocate`) refusait de dispatcher un job tant que la VRAM libre était insuffisante — sans savoir que l'occupant était **notre propre LLM d'arbitrage inactive** (gardée chaude, étalée sur les GPU). Le job restait en `waiting` pour toujours, le reclaim ne tournant jamais. Désormais, à l'admission, quand une phase **non-LLM** (STT/diarisation — la phase `llm_arbitration` est déjà ignorée via `llm_shared`) manque de VRAM, le scheduler **arrête proprement notre LLM d'arbitrage inactive** (catégorie 1, verrou LLM libre, toujours autorisée — relancée à la phase de correction) puis re-teste l'allocation. Logique mutualisée avec le runner via `transcria/gpu/vram_reclaim.stop_idle_arbitrage_llm`.

### Added
- **Politique de préemption VRAM `gpu.preemption`** (`own-only` par défaut | `aggressive`) — réglable dans l'**éditeur de configuration** (`/admin/config`, section « Ressources GPU »). `own-only` (recommandé, infra partagée) : n'arrête que **nos** process gérés inactifs (LLM d'arbitrage trackée), **jamais** un process tiers. `aggressive` : préempte aussi les serveurs d'inférence tiers (`kill_patterns`, process non trackés) — **uniquement dans la fenêtre calendaire `force_gpu`** —, à réserver à un GPU dédié à TranscrIA. Remplace l'appel `force_free_gpu` inconditionnel-en-fenêtre de l'admission par cette politique explicite (défaut plus conservateur qu'avant : plus de kill de process tiers hors `aggressive`). `_run_quick_transcription` réservait toujours la VRAM locale (`summary_stt`, 6000 Mo via `_gpu_session`) **même quand le STT du résumé est routé vers un nœud de ressources distant** (`inference.mode` ∈ remote/hybrid). Sur un tier sans GPU, cela provoquait une fausse contention / une mise en attente VRAM à tort. Désormais le résumé saute la réservation locale quand `summary_stt` est une phase distante (`WorkflowRunner._phase_runs_remotely`), alignant le résumé sur le pipeline principal (`_reserve_gpu_phase`). Cf. `docs/SERVICE_RESSOURCES_GPU.md` §9.
- **Échec silencieux de la LLM d'arbitrage (résumé corrompu sans alerte)** : si opencode se terminait « avec succès » mais **sans produire de texte** (« 0 textes » — cause fréquente : transcript trop long), il ne réécrivait pas `summary.md` et le **placeholder** de `SummaryGenerator` était relu, parsé, puis stocké comme s'il était le résumé (`meeting_context["summary_llm"]` corrompu), le job passant `SUMMARY_DONE` en silence (incident `e62295c1`). Désormais : `OpenCodeRunner.run_summary` détecte la non-production de façon robuste (mtime de `summary.md` **avant/après** le run, plus de matching de chaîne fragile) et expose `_summary_produced`. `WorkflowRunner._run_llm_summary` **retente la seule phase LLM jusqu'à 3 fois** (LLM déjà chargée : pas de re-STT). Après 3 échecs : `meeting_context` **non corrompu**, job **non** `SUMMARY_DONE` (drapeau `extra_data.summary_llm_failed`), bandeau d'erreur dans le wizard et **résumé relançable** — la relance **réutilise le transcript en cache** (`quick_transcript.txt`/`summary.json`) au lieu de refaire le STT GPU.
- **VRAM bloquée par notre propre LLM d'arbitrage inactive** : après un résumé synchrone, la LLM d'arbitrage restait chargée (aucun idle-stop) et, étalée sur les GPU via `--tensor-split`, empêchait un STT de réserver sa VRAM — bloquant indéfiniment la reprise `waiting_vram`. Désormais, sur pénurie VRAM d'un STT (résumé/transcription/diarisation), `WorkflowRunner._reclaim_vram_from_idle_arbitrage_llm` **arrête notre LLM d'arbitrage uniquement si elle est inactive** (verrou LLM libre — garde-fou multi-job : jamais une LLM en service) puis **retente une fois** la réservation. C'est notre process géré (jamais un process tiers). La LLM reste sinon chaude pour le job suivant.

### Added
- **`transcria doctor --llm-smoke`** : test **opt-in** qui lance réellement opencode → LLM avec une consigne triviale et vérifie qu'il **produit du texte** — attrape a priori la panne « 0 texte » ci-dessus (`check_opencode_smoke`, réutilise la capture de log d'arbitrage). Nécessite la LLM up et consomme de la VRAM : volontairement hors du préflight par défaut (qui reste GPU-free et sans effet de bord).

### Changed
- **VRAM insuffisante → mise en attente au lieu d'échec terminal** : quand la VRAM locale manque pour une phase GPU (STT rapide cohere-summary, transcription, diarisation, détection de locuteurs), le job ne passe plus en **FAILED** (état terminal + e-mail d'échec au propriétaire + alerte rouge). Une indisponibilité VRAM est *transitoire* : le job est désormais marqué **« en attente de VRAM »** (statut d'exécution `waiting_vram`, non terminal) et **reprend automatiquement** dès que la mémoire se libère — sur le même principe que le mode `deferred` des ressources distantes (§7.2). Côté **file principale** : `WorkflowRunner` (transcription/diarisation/locuteurs) et `PipelineService` remontent un signal `vram_wait` ; `JobExecutorService._run_process` re-queue le job (`QueueStore.requeue_later`) sans état terminal et le scheduler le redispatche dès admission VRAM possible. Côté **résumé synchrone** (« Générer le résumé ») : la 1ʳᵉ tentative reste synchrone ; sur `vram_wait`, `api_summary` restaure l'état pré-résumé et **enfile une reprise serveur** (mode de file `summary`, profil VRAM dédié) que le **scheduler reprend automatiquement dès libération de la VRAM, même sans page ouverte**. `JobExecutorService._run_process` exécute alors `run_summary` (qui gère l'état `SUMMARY_DONE`/`FAILED`) sans marquer `COMPLETED` ni notifier le propriétaire. Le wizard affiche un bandeau d'attente et **poll `GET /status`** jusqu'à `summary_done` (plus de re-POST client → aucune double-exécution). Dans tous les cas, **l'administrateur est alerté une seule fois** par épisode (e-mail `send_admin_vram_alert_async` aux comptes ADMIN actifs + log `WARNING` structuré + bandeau in-app comptant les jobs `waiting_vram`). TranscrIA continue de **ne jamais tuer** un process GPU tiers (le bridage de `force_free_gpu` est inchangé). Voir `docs/SERVICE_RESSOURCES_GPU.md` §7.2.

### Fixed
- **Invitation de réunion : affichage fiabilisé (pas une perte de données)** : le brief d'invitation collé était bien sauvegardé (`extra_data.meeting_invite.brief`), mais le `<textarea>` était toujours rendu vide et le badge de confirmation ne s'affichait que si des **noms** étaient extraits d'e-mails `prenom.nom@` — sans e-mail parsable, un brief enregistré semblait perdu. Désormais : le textarea est **pré-rempli** avec le brief stocké, le badge « Invitation enregistrée » s'affiche dès qu'un **brief OU des noms** existent, et le collé est **auto-sauvegardé au `blur`** (plus seulement au clic « Générer le résumé ») pour survivre à une étape échouée/abandonnée.
- **Diagnostic d'une LLM d'arbitrage qui ne démarre pas** : le lancement du serveur LLM (`VRAMManager.launch_arbitrage_llm`, `ScriptLLMBackend.ensure_available`) redirigeait stdout/stderr vers `/dev/null`, puis se contentait d'attendre l'ouverture du port jusqu'à **600 s** avant de logguer un timeout sec. Conséquence (constatée sur une autre machine après un `git pull`) : quand le serveur échouait au démarrage (chemin de binaire faux, OOM GPU, `--tensor-split` incompatible avec le nombre de GPUs…), la LLM restait « down » **sans aucun log expliquant pourquoi** — seul restait le placeholder « Résumé indisponible (LLM non configurée) ». Désormais : la sortie du script est **capturée** dans `services.arbitrage_log_path` (défaut `/tmp/arbitrage_llm_<port>.log`) ; l'attente du port **détecte la mort précoce du process** (`proc.poll()`) et abandonne immédiatement au lieu d'attendre tout le timeout ; en cas d'échec (mort précoce **ou** timeout), le code de sortie et les **dernières lignes du log** sont loggués en `ERROR`. Aligne le backend LLM sur la convention déjà utilisée par le superviseur STT. Voir `docs/CONFIG_REFERENCE.md` (note d'exploitation « Diagnostic d'un démarrage LLM raté »).
- **Enqueue idempotent face à une course double-submit** : deux requêtes simultanées sur le **même** `job_id` (double-clic / session partagée) pouvaient toutes deux passer les gardes (`is_execution_active`/`already_active`, lecture avant écriture) puis tenter l'`INSERT` ; la contrainte unique `job_queue.job_id` préservait déjà la correction (pas de doublon ni de double-run) mais le perdant levait une `IntegrityError` non capturée → 500. `QueueStore.enqueue` capture désormais l'`IntegrityError`, fait un `rollback`, relit l'entrée gagnante et la réutilise (helper partagé `_refresh_entry`) — course gracieuse, plus de 500. Une `IntegrityError` sans rapport avec l'unicité `job_id` est re-levée (pas de masquage).
- **Fourchette de locuteurs honorée en diarisation distante** : la contrainte de locuteurs par job (`speaker_hint` → `num_speakers`/`min_speakers`/`max_speakers`) était **silencieusement ignorée** quand `models.diarization_backend=remote` (régression vs mode local). Elle est désormais transmise de bout en bout : `RemoteDiarizer` lit `_effective_speaker_params()` → `InferenceClient.diarize(..., speaker_params)` (JSON en file_ref, champs de formulaire en upload) → route `/infer/diarize` (extraction + validation) → `DiarizeEngine.diarize(speaker_params=…)` → `DiarizerService.diarize_audio(speaker_params=…)`, où la contrainte par appel **prime sur la config statique du nœud** (re-validée au bord réseau via le validateur partagé `_normalize_speaker_params`). Parité local↔distant rétablie.

### Added
- **Préflight de diagnostic `transcria doctor`** : commande **GPU-free et sans effet de bord** (`venv/bin/python scripts/doctor.py`, ou `python -m transcria.diagnostics.doctor`) qui détecte *avant* exécution les pannes classiques qui sinon se traduisent par des jobs en échec sans cause lisible — config illisible, **schéma de base dérivé** (table/colonne attendue par les modèles absente de la base, ex. `job_queue.error_message` après un `git pull` sans `alembic upgrade head`, via `alembic.autogenerate.compare_metadata` sur la base réelle), script de lancement LLM introuvable/non exécutable, LLM d'arbitrage injoignable (rappelle le log à lire) ou modèle actif ≠ `arbitrage_api_model_id`, binaire `opencode` absent alors qu'une phase LLM est activée, nœud de ressources distant injoignable en mode `remote`/`hybrid`, dossiers de travail non inscriptibles. Chaque vérification est isolée (une exception devient un `fail` explicite, jamais un crash) et chaque ligne `WARN`/`FAIL` affiche une piste de correction. Options `--config`, `--json` (outillage/CI), `--strict` (avertissements = échec) ; code de sortie 0/1 utilisable en script de déploiement. Logique pure et injectable dans `transcria/diagnostics/doctor.py` (31 tests). Voir `docs/INSTALL.md` §12.
- **Éditeur de configuration à formulaires (`/admin/config`)** : l'édition de la config ne se limite plus à un `<textarea>` de YAML brut. Un onglet **Réglages** offre des formulaires lisibles (libellé + aide + validation par type) pour les paramètres les plus courants (modèles/backends, LLM d'arbitrage, file & exécution, sécurité/upload, notifications email, voix, serveur & compte admin), tandis qu'un onglet **YAML (avancé)** conserve l'édition complète. Spécification déclarative dans `transcria/web/config_form.py` (`CONFIG_FORM_SECTIONS` + helpers purs) ; un formulaire ne soumet qu'un dict partiel fusionné dans la config complète (aucune clé perdue), validé par `ConfigService.save_if_valid` et audité (`CONFIG_EDIT`). Secrets masqués/préservés. Test anti-dérive : chaque chemin de formulaire doit exister dans la config par défaut.
- **Corpus de calibration difficulté↔qualité STT par segment (brique 2)** : module pur `transcria/stt/corpus.py` (`difficulty_for_range`, `build_segment_corpus`, `summarize_corpus`). Après le scoring de fiabilité, `Transcriber` joint chaque segment transcrit à la `difficulty_map` par fenêtre et écrit `metadata/stt_corpus.json` (difficulté × moteur × confiance native × fiabilité par segment). Un agrégat compact (contingence difficulté×fiabilité, moyennes de confiance) est promu dans `extra_data.stt_corpus_summary` pour le requêtage cross-jobs. Activé par défaut (`workflow.stt_corpus.enabled`), best-effort (n'interrompt jamais la transcription). Complète la brique 1 (`audio_summary`). Voir `docs/STT_ADAPTATIF_ET_HYBRIDE.md`.
- **Proxy qualité du corpus STT (`quality_measure`)** : le 3ᵉ axe du corpus (qualité mesurée) est rempli automatiquement par un proxy WER — le taux d'édition mot-à-mot entre SRT brut et SRT corrigé (`segment_edit_rate` via `difflib`). `WorkflowRunner._enrich_stt_corpus_quality()` s'exécute en début de phase qualité (SRT corrigé définitif), apparie segments↔blocs SRT par timecode (`parse_srt_blocks`, recherche dichotomique quasi-linéaire), réécrit `stt_corpus.json` et enrichit `stt_corpus_summary` d'un `edit_rate_mean` par niveau de difficulté (table de calibration `difficulté → taux d'édition`). Best-effort, invisible (pas de step pipeline), ~49 ms sur un job 1h52. Proxy conservateur — exploiter en relatif ; une vérité terrain annotée pourra le remplacer (même schéma).
- **Frise temporelle du diagnostic audio** : l'encart diagnostic du wizard affiche désormais une barre temporelle colorée (ok/suspect/degrade) par fenêtre de la `difficulty_map`, bornée à 160 segments, avec tooltips (plage horaire + raisons lisibles), axe de temps et légende. Helper pur `_build_difficulty_frise()` dans `web/routes.py`.
- **Base de données PostgreSQL (Phase A)** : bascule de SQLite vers **PostgreSQL** (driver `psycopg`) pour tenir la concurrence. Schéma géré par **Alembic** (`alembic upgrade head` lancé par `start.sh` ; `db.create_all()` réduit au bootstrap dev/tests) ; test anti-dérive `tests/test_alembic_migrations.py`. DSN via `TRANSCRIA_DATABASE_URL` (prioritaire, garde le mot de passe hors config versionnée), pool dialecte-aware. Colonnes temporelles en `TIMESTAMPTZ`, `audit_logs.actor_id` en `ON DELETE SET NULL`. Migration des données : `scripts/migrate_sqlite_to_postgres.py`. Tests sur cluster PostgreSQL éphémère (`pytest-postgresql`). SQLite reste un repli mono-process pour le dev. Voir `docs/INSTALL.md` §7.
- **Montée en charge & concurrence (Phase B)** : exploite PostgreSQL pour scaler sans double-exécution ni sur-réservation VRAM. Conception complète dans `docs/CONCURRENCE_ET_CHARGE_PHASE_B.md`.
  - **Rôles de process** `runtime.role` / `TRANSCRIA_ROLE` ∈ `all` (tout-en-un, défaut) | `web` (tier HTTP sans état, **gunicorn -w N**, `wsgi.py`) | `scheduler` (orchestrateur unique). Garde-fou « ordonnanceur unique » par **verrou consultatif PostgreSQL** (`transcria/queue/scheduler_lock.py`). Unités systemd `deploy/transcria-{migrate,web,scheduler}.service` + `deploy/nginx-transcria.conf.example`.
  - **Claim de job atomique** `FOR UPDATE SKIP LOCKED` (`QueueStore.claim`) : pas de double-dispatch même multi-worker ; capacité lue en base (`count_running`).
  - **Failover actif/passif des nœuds de ressources** : `inference.nodes` (liste ordonnée), `FailoverInferenceClient` qui bascule par priorité sur `InferenceUnavailable` (pas sur 4xx), recalculé par appel (pas de split-brain).
  - **Admission VRAM-aware + backpressure à l'échelle** : aging ensembliste (un seul `UPDATE`), capacité du nœud au dispatch (`available_remote_slots`), admission selon le coût VRAM local/distant (`_resources_available` + `remote_vram_admits`), index partiel `ix_job_queue_waiting_order`, cache court de `GET /api/resources/status` (`inference.resilience.capabilities_cache_ttl_s`).
  - **Profil de concurrence & observabilité du goulot** (`transcria/workflow/concurrency_profile.py`) : classe les étapes *serial*/*delegated*, mesure le % sériel et l'étape goulot, expose une attente estimée dans `GET /api/resources/status` (clé `concurrency`). Surcharges via `workflow.concurrency_profile`.
  - **Réveil instantané optionnel** `LISTEN/NOTIFY` (`workflow.queue.use_listen_notify`, `transcria/queue/notify_listener.py`) : le polling reste le filet de sûreté.
  - Durcissement du nœud de ressources : `ensure_ready` sérialisé par moteur, état de charge dans `/capabilities` (`capacity/inflight/queued/last_wait_s`, `ensure_in_progress`), invariant single-process documenté.
- **Qualification du son avant transcription (axe 1)** : caractérisation acoustique enrichie pour mieux décider de la difficulté d'un audio. `squim_scorer.py` (STOI/PESQ/SI-SDR via torchaudio), `dnsmos_scorer.py` (SIG/BAK/OVRL P.835 via onnxruntime, modèle ONNX embarqué), `acoustic_metrics.py` (RT60 sur décroissances libres, C50, SNR fenêtré, codec VoIP), fusion pondérée `difficulty_map.py` (avec veto overlap). Branchés dans le préflight (globaux + par fenêtre, lazy). Encart diagnostic dans le wizard (scores SQUIM/DNSMOS, conseil bruit-vs-parole, résumé temporel). Actifs par défaut via `workflow.audio_preflight.{squim,dnsmos,acoustic}`. Attribution : `THIRD_PARTY_NOTICES.md` (poids CC-BY-4.0).
- **Rapport Word (.docx) adapté au type de réunion** : document professionnel généré en fin de workflow (téléchargeable / inclus au ZIP). Extraction structurée par la LLM (décisions, actions, points bloquants/reportés, votes, résolutions, ordre du jour) avec dégradation gracieuse ; **18 types de réunion** (CSE, CODIR/COMEX, point projet, réunion client, crise…) à champs et thèmes visuels dédiés (calcul du quorum CSE, badge confidentiel/crise). Voir `docs/FEATURE_DOCX_REPORT.md`.
- **Inférence distante & autonomie VRAM du STT (v1)** : TranscrIA peut opérer en frontale dont les ressources GPU (STT, diarisation, empreinte vocale) tournent sur un nœud distant — ou en tout-en-un. Conception complète dans `docs/SERVICE_RESSOURCES_GPU.md`.
  - STT distant via protocole **OpenAI** `/v1/audio/transcriptions`, moteur de serving **non hardcodé** (vLLM/SGLang/custom) : `RemoteTranscriber` + `AsrClient` ; sélection par `inference.mode` + `inference.stt.backends[*].url` ; `response_format` par backend ; conversion WAV 16k mono (contourne le bug MP3) ; concurrence par tour (`inference.stt.concurrency`).
  - Autonomie VRAM du STT (cycle A/B/C, non intrusif) : `SttVramPlanner` (pré-check fraction×total vLLM + relocalisation GPU), `SttEngineSupervisor`, route `POST /engines/ensure` (lance à la demande / 503 si saturé). Détection via `GET /capabilities`.
  - Admission en mode dégradé (§7.2) : `resource_gate.prepare_remote_resources()` en pré-vol de `PipelineService.run_process` (poursuit / file / échec explicite borné par `inference.resilience.max_unavailable_s`) — jamais d'échec silencieux ni de spin. Panneau d'état : `GET /api/resources/status` + carte dans `dashboard_status.html`.
  - Correctif : une phase servie à distance ne réserve plus de VRAM locale (`WorkflowRunner._phase_runs_remotely`).
  - **Re-queue différé (§7.2)** : un job dont les ressources distantes sont transitoirement injoignables est replanifié (`QueueStore.requeue_later` + `scheduled_at`) au lieu d'échouer ; il re-tente après backoff jusqu'au succès ou échec explicite au-delà de `inference.resilience.max_unavailable_s`. `job_executor` gère le verdict `deferred` (état `queued`, pas de FAILED).
  - **Idle-stop minimal (v1.2)** : un moteur STT externe déclaré avec `idle_timeout_s > 0` et inactif au-delà du seuil est arrêté (`SttEngineSupervisor.reap_idle` via `scripts/stop_stt.sh`), de façon opportuniste (poll `/capabilities` + `ensure_ready`, sans tâche de fond). Non intrusif : ne touche que les moteurs servis par le service. Défaut `idle_timeout_s=0` = résident.
  - Scripts : `_stt_serve_lib.sh`, `launch_stt_{cohere,whisper,granite}.sh`, `stop_stt.sh`, `test_stt.sh`, `scripts/smoke_remote_stt.py`. Banc E2E : options `--remote-stt` / `--remote-inference`.
- Architecture diarisation modulaire : `BaseDiarizer` (ABC, `transcria/stt/base_diarizer.py`) factorise la logique partagée entre tous les backends — cache, extraits audio, empreintes acoustiques, fingerprint, chargement GPU. `DiarizerService` (pyannote) et `SortformerDiarizer` (NeMo) héritent de cette classe de base. `create_diarizer(config, device)` et `get_diarizer_vram_mb(backend, config)` centralisent la construction et la réservation VRAM dans `transcria/stt/diarizer_factory.py`. `list_available_backends()` retourne la liste des backends enregistrés (`pyannote`, `sortformer`). `runner.py` et `speaker_detection.py` utilisent désormais la factory au lieu d'instancier `DiarizerService` directement.
- Backend de diarisation Sortformer (`SortformerDiarizer`, `transcria/stt/sortformer_diarizer.py`) : NVIDIA diar_streaming_sortformer_4spk-v2.1 via NeMo, jusqu'à 4 locuteurs, sortie exclusive (pas de chevauchements), ~3.5 Go VRAM. Activé via `models.diarization_backend=sortformer`. `_parse_sortformer_output()` convertit les lignes `"start end speaker_N"` produites par `model.diarize()` en dict `{start, end, speaker, duration}`. `_normalize_speaker_id()` normalise `speaker_0` → `SPEAKER_00`. Fallback gracieux si NeMo est absent.
- Nouvelles clés de configuration pour le backend Sortformer : `models.diarization_backend` (par défaut `"pyannote"`) sélectionne le backend de diarisation ; section `sortformer.*` (`model_id`, `vram_mb`) ; `gpu.sortformer_vram_mb` (défaut `3500`).
- 37 nouveaux tests diarisation : `TestSortformerDiarizerParseOutput` (9 tests purs de parsing/normalisation), `TestSortformerDiarizerConfig` (6 tests instanciation, disponibilité, fallback NeMo), `TestDiarizerFactory` (9 tests factory : pyannote/sortformer/fallback/device/VRAM/liste). Tests `TestDiarizerServiceConfigInit` : `test_is_base_diarizer_subclass` ajouté (vérification héritage).
- Voix enregistrées avec consentement : nouveau module `transcria/voice/` (sujets, consentements, profils, matches, audit), menu admin/admin groupe `/admin/voices`, formulaire PDF vierge `/admin/voices/consent-form.pdf`, stockage sensible `voices/`, empreintes vocales locales avec suppression de l'audio source par défaut, matching manuel des locuteurs via `/api/jobs/<id>/speakers/voice-match`, suggestions affichées dans l'étape Participants & Locuteurs sans attribution automatique.
- Voix enregistrées : genre validé sur la fiche voix, propagation du genre dans les suggestions de matching, consultation sécurisée de la preuve signée, et protection contre l'écrasement d'un nom validé par les suggestions LLM.
- E2E applicatif `tests/test_voice_e2e.py` couvrant téléchargement du PDF vierge, création d'une voix, upload du consentement, génération d'empreinte mockée, matching d'un locuteur et affichage de la suggestion dans le wizard.
- Attribution automatique du genre par locuteur : `WorkflowRunner._assign_speaker_genders(gender_segments, turns, min_overlap_s)` (méthode statique pure) croise les segments genre horodatés d'`audio_scene.json` avec les tours pyannote de `speaker_turns.json` pour voter le genre dominant de chaque SPEAKER_XX. `_inject_speaker_genders(fs, audio_scene)` orchestre la lecture depuis le disque, met à jour `speakers/speaker_stats.json` (champ `gender`, jamais écrasé si déjà rempli par l'utilisateur) et logue le détail timing + résultat par locuteur. Appelée depuis `_run_pyannote_after_transcription` (flow résumé) ET depuis `run_diarization` (pipeline qualité, après que `audio_scene.json` soit sur disque). `_write_diarization_context` accepte un nouveau paramètre `speaker_genders` et insère une section Markdown "Genre vocal par locuteur (estimation acoustique)" pour le LLM.
- `_scene_analysis_worker.py` : champ `gender_segments` ajouté au JSON de sortie — liste `[{start, end, label}]` filtrée `male|female` uniquement. Rétrocompat totale (consommateurs existants ignorent le nouveau champ).
- 19 nouveaux tests : `TestSceneWorkerGenderSegments` (`test_audio.py`, 4 tests), `TestAssignSpeakerGenders` (8 tests purs), `TestInjectSpeakerGenders` (4 tests intégration fs), `TestWriteDiarizationContextWithSpeakerGenders` (3 tests).
- Intégration pipeline analyse de scène + séparation de sources : `PipelineService._run_audio_scene_analysis()` et `_run_source_separation()` s'exécutent avant la transcription finale. L'analyse de scène tourne dans un subprocess CPU isolé (librosa) qui se termine avant le chargement GPU. La séparation de sources utilise les signaux de scène (`has_music`) comme déclencheur prioritaire.
- Contexte de diarisation enrichi du genre vocal : `WorkflowRunner._build_gender_section(audio_scene)` génère les lignes Markdown de distribution H/F (ratio, dominant, durées) et les injecte dans `summary/diarization_context.md` si des données de genre sont disponibles.
- UI genre locuteur : bannière "Analyse acoustique globale" affichant les ratios H/F dans l'étape Participants (visible si `audio_scene.gender.has_gender_data`) ; select genre (Non déterminé / Féminin ♀ / Masculin ♂) par ligne de locuteur, valeur persistée dans `speakers/speaker_stats.json` via `SpeakerDetector.save_mapping()` (champ `gender`).
- `tests/test_pipeline_service.py` : 8 nouveaux tests couvrant l'analyse de scène (disabled/unavailable/exception/success), la séparation (decider_no/service_success/service_fails) et l'ordre des étapes (scene+sep avant transcription).
- `tests/test_workflow_runner.py` : 7 nouveaux tests `TestDiarizationContextGenderSection` couvrant `_build_gender_section` et `_write_diarization_context` avec/sans données de genre.
- `tests/test_audio.py` : tests worker scène pure (`TestAudioSceneWorkerStats`, 6 tests) + tests `AudioSceneAnalyzer` subprocess (5 tests) + 3 tests `SourceSeparationDecider` avec signaux de scène (musique prioritaire, absence de musique, fallback score).
- Mode Whisper qualité : `workflow.quality_transcription` force Whisper large-v3 en mode qualité et peut le forcer automatiquement sur diagnostic de son dégradé.
- Paramètres Whisper enrichis : timestamps mot-à-mot, seuils hallucination/logprob/compression, hotwords, prompt initial, suppression numérique optionnelle et réduction des boucles répétitives.
- Anti-hallucination ASR natif : `transcria/stt/anti_hallucination.py` détecte et réduit les boucles de répétition configurables.
- Décision qualité audio : `transcria/quality/audio_quality.py` agrège `metadata/audio_analysis.json` et `summary/summary.json`, écrit `metadata/audio_quality_decision.json` et pilote la bascule Whisper.
- VAD adaptatif : `transcria/audio/vad_adaptive.py` ajuste les seuils Silero selon la décision qualité audio.
- Alignement forcé CTC natif : `transcria/stt/forced_alignment.py` utilise `torchaudio_ctc` sans dépendance WhisperX, désactivé par défaut.
- Wildcard CTC : `_text_to_chars()` et `_build_wildcard_emission()` permettent d'aligner des segments contenant des chiffres, de la ponctuation ou tout caractère absent du vocabulaire du modèle, en étendant la matrice d'émission d'une colonne wildcard (max des scores non-blank par trame) au lieu de les supprimer silencieusement.
- Séparation de sources audio : `transcria/audio/source_separation.py` — `SourceSeparationDecider` analyse les signaux de `audio_quality_decision.json` (score VAD/hallucinations) et les signaux de scène audio (`audio_scene`) pour décider si Demucs doit être appliqué ; musique détectée → séparation prioritaire quel que soit le score. `SourceSeparationService` extrait la piste vocale avec dégradation gracieuse (demucs absent, exception → audio original conservé). Non déclenché automatiquement. Configurable via `workflow.source_separation`.
- Analyse de scène audio : `transcria/audio/scene_analyzer.py` + `transcria/audio/_scene_analysis_worker.py` — pipeline acoustique en cascade (énergie RMS → classification spectrale speech/music/noise via flatness/ZCR → estimation de genre par pitch YIN) exécuté dans un subprocess isolé. Libère la mémoire avant le chargement GPU pyannote/Whisper. Produit `has_music`, `has_noise`, `speech_ratio` et distribution H/F. Aucune dépendance modèle ML externe : utilise librosa déjà présent dans les requirements. Configurable via `workflow.audio_scene` (seuils, timeout, detect_gender).
- Réalignement locuteur/ponctuation : `transcria/stt/speaker_realignment.py` scinde les segments par timestamps mots et tours pyannote.
- Checkpoints pyannote : `speakers/diarization_checkpoint.json` permet la réutilisation des tours, `speakers/speaker_embeddings.json` stocke un checkpoint acoustique par locuteur.
- Gestion des mots de passe : route `/account/password` pour changement par l'utilisateur avec mot de passe actuel, confirmation et longueur minimale ; reset admin clarifié avec confirmation.
- Gestion des groupes utilisateurs : tables `groups` / `group_memberships`, `GroupStore`, routes `/admin/groups`, admins de groupe, visibilité automatique des jobs entre membres d'un même groupe.
- Scripts LLM génériques : `scripts/stop_llm_backend.sh` (arrêt par port/PID file/pattern) et `scripts/stop_arbitrage_llm.sh` (wrapper standard). `stop_qwen.sh` et `stop_qwen_vllm.sh` restent des wrappers legacy.
- Configuration LLM générique : `services.arbitrage_llm_port`, `services.llm_cleanup_ports`, compatibilité lecture avec `qwen_port` et `vllm_port`.
- Configuration VAD fine : `workflow.vad.enabled_summary`, `enabled_final`, `threshold`, `min_speech_duration_ms`, `min_silence_duration_ms`, `speech_pad_ms`.
- Diagnostics summary ASR/VAD dans `summary/summary.json` (`level`, `flags`, ratio parole, segments courts, segments non latins).
- Garde-fous qualité SRT dans `QualityReporter` : noms de locuteurs modifiés, segments marqués étrangers, segments non latins, segments courts suspects et `review_load`.
- `quality.asr_noise_markers` : marqueurs configurables de bruit ASR court, sans modification de code.
- Support `audio_array` dans `WhisperTranscriber`, aligné avec l'interface Cohere ; Whisper est maintenant activé par le mode qualité ou par diagnostic audio dégradé.
- `docs/VAD_PYANNOTE_PISTES.md` : documentation complète sur le VAD Silero (avantages, inconvénients, métriques de détection de dégradation) et le tuning pyannote (hyperparamètres, méthode sans/avec annotations), plus pistes d'amélioration globales (checkpoint embeddings, fine-tuning ASR, session opencode persistante, validation format LLM).
- `WorkflowRunner._extract_name_hints()` (runner.py) : nouvelle méthode statique qui analyse les segments labellisés pour extraire deux types d'indices d'identification des locuteurs — apostrophes directes (locuteur A termine son tour en appelant B par son prénom juste avant que B prenne la parole) et noms propres en milieu de phrase par locuteur (mots capitalisés hors début de phrase, hors sigles, hors mots fonction). Les indices sont injectés dans une section dédiée de `diarization_context.md`.
- `diarization_context.md` enrichi : extraits de segments portés de 120 à 200 caractères (`_build_labeled_segments`), nouvelle section `## Indices pour identifier les prénoms des locuteurs` avec sous-sections `### Apostrophes directes détectées` et `### Noms propres en milieu de phrase par locuteur`, consigne enrichie (priorité aux apostrophes directes, auto-désignation comme indice fort).

### Removed
- Permission morte `MANAGE_QUEUE` supprimée : elle était définie et accordée à `admin` mais jamais vérifiée (la file utilise `_can_manage_queue()`/`_can_manage_queue_job()`, qui couvrent admins globaux **et** admins de groupe selon le périmètre). `docs/DATA_MODEL.md` mis à jour.

### Fixed
- **Page Système qui pendait** : `DashboardClient.get_system_status()` court-circuite désormais dès le 1er appel en échec (au lieu d'enchaîner 4 requêtes qui timeout) et le timeout par défaut passe de 10 s à 3 s. En tout-en-un (dashboard de ressources absent), `/system` répond immédiatement en mode dégradé au lieu d'attendre ~40 s ; un warning est logué.
- **Verrouillage du dernier administrateur empêché** : `user_edit` refuse désormais de rétrograder ou désactiver le dernier administrateur global actif (`UserStore.count_active_admins()` + helper pur `_removes_last_active_admin`), avec message et log. Aligne le comportement sur la garde déjà présente pour les admins de groupe.
- **Audit des membres de groupe** : l'ajout et le retrait d'un membre de groupe (qui modifient la visibilité croisée des jobs) sont maintenant journalisés (`group_member_add`, `group_member_remove`), comme les autres opérations de groupe.
- `WorkflowRunner._apply_speaker_roles()` : propagation des noms LLM vers `speaker_stats.json` et `speaker_mapping.json` déplacée hors du bloc `if updated or created:` — elle était ignorée quand `participants.json` était déjà à jour, empêchant l'affichage des noms dans l'étape Locuteurs.
- `web/routes.py` enrichissement étape 5 : le champ `mapped_name` n'était pas mis à jour depuis `participants.json` — les noms saisis dans Participants n'apparaissaient pas dans la liste Locuteurs. Fix : mise à jour `mapped_name` avec protection anti-SPEAKER_XX.
- Pré-remplissage Participants & Locuteurs : séparation robuste entre `label` et `role` pour les formats `SPEAKER_XX [Fonction A] : rôle` et `SPEAKER_XX : Fonction A — rôle`; l'UI et `_apply_speaker_roles()` normalisent aussi les anciens résumés déjà produits.
- `SpeakerDetector.save_mapping()` réécrit `speaker_stats.json` après mapping afin d'éviter un état stale `validation: pending`.
- Généralisation des arrêts de backends LLM concurrents : plus de dépendance structurelle à vLLM dans le chemin principal.
- Parser `_parse_structured_summary()` : les vrais placeholders `non identifiable` sont ignorés, mais un rôle contenant ces mots n'est plus rejeté quand la ligne contient un vrai `SPEAKER_XX [label]`.

### Added
- `summary_prompt.txt` v2.0 : restructuration complète — 10 sections numérotées, double passe formalisée (§5), règles explicites quoi lister / quoi exclure (§6.1/6.2), hiérarchie `### Passe 1` / `### Passe 2` correcte, `(non identifiable)` aligné sur le parser Python, séparateur `||` pour contextes multiples, exemples de format (§6.9), `Tu dois utiliser les outils Read et Write` en §10, vérification finale 12 points. Compatibilité totale avec `_parse_structured_summary()`.
- `correction_prompt.txt` v1.7 : vérification par sous-agent (section 15) — après écriture des deux fichiers, un sous-agent relit le SRT corrigé et le lexique depuis le disque, croise avec les corrections déclarées dans le rapport, corrige les variantes restantes ou les hallucinations de rapport, et ajoute une section `## Vérification sous-agent` dans `correction_report.md`. Étape 13 ajoutée à la procédure d'exécution.
- `correction_prompt.txt` v1.6 : anti-split SRT — règles absolues dans SORTIES (totalité des segments dans un seul `transcription_corrigee.srt`, séparation stricte SRT/Markdown entre les deux fichiers), checks 11 et 12 en VÉRIFICATION FINALE (complétude SRT, séparation fichiers), INSTRUCTIONS mises à jour. Instruction inline `run_correction()` (opencode_runner.py) renforcée avec les mêmes contraintes.
- `correction_prompt.txt` v1.5 : règle `mapped_name` immuable — le modèle recopie le `mapped_name` verbatim (casse, accents, orthographe) sans aucune normalisation ni interprétation. Trois niveaux de défense : définition en Section 1, extraction préalable de la table `speaker_id → mapped_name` avant tout segment (Étape B de la PREMIÈRE ACTION), vérification finale (check 10). Clarification de la sémantique de `replace_by` vide.
- Logs WARNING dans `_parse_structured_summary()` (opencode_runner.py) pour les cas d’échec de parsing : sections `## Participants probables` et `## Termes suspects/douteux` introuvables, champs critiques non extraits (`title_suggere`, `type_suggere`, `sujet_suggere`), termes à zéro malgré section présente.
- Logs WARNING dans `_apply_llm_suggestions()` (runner.py) quand le résumé est indisponible (sentinel) et quand des champs LLM restent vides après parse.
- Logs WARNING dans `run_summary()` (opencode_runner.py) pour les chemins de fallback (`summary.md` absent, repli sur glob `*.md` ou stdout) et le résumé vide après exécution opencode.
- Pré-remplissage automatique des rôles participants : la LLM de résumé détecte les rôles SPEAKER_XX et les écrit dans `context/participants.json` après la création du mapping locuteurs (section 5 du wizard pré-remplie).
- Stockage des rôles LLM dans `meeting_context.json[“speaker_roles_llm”]` pour persistance inter-phases et réapplication après mapping.
- Récupération des processus opencode orphelins au démarrage du service (`job_executor._kill_orphaned_opencode`) — les jobs interrompus pendant une inférence LLM ne bloquent plus les GPUs.
- Suivi des PID opencode par fichier `.opencode.pid` dans le répertoire du job (jamais de kill aveugle par nom de processus).
- `OpenCodeRunner` utilise `subprocess.Popen` + `_terminate_proc()` (SIGTERM puis SIGKILL) à la place de `subprocess.run()` qui ne tuait pas le processus en cas de timeout.
- Endpoint `POST /api/jobs/<id>/speakers/map` réapplique maintenant les rôles LLM en production après la création du mapping.
- Parser `_parse_structured_summary()` supporte deux formats SPEAKER_XX : `SPEAKER_XX [label] : rôle` (Format A) et `SPEAKER_XX : rôle` (Format B sans label).
- `summary_prompt.txt` v1.3 : chasse systématique aux variantes STT en deux passes, format SPEAKER_XX obligatoire si diarization fournie.
- Test E2E : pyannote lancé en pré-résumé si `diarization_context.md` absent, puis rôles appliqués après l’étape mapping.

### Changed
- `PipelineService._define_pipeline_steps()` utilise `functools.partial` au lieu de lambdas pour rendre les captures d'arguments explicites.
- Documentation README/TODO/install/technique clarifiée : Qwen est un exemple historique ou un alias legacy, pas une dépendance applicative de la LLM d'arbitrage.
- Worker interne sérialisé pour exécuter les traitements longs hors de la requête HTTP.
- Endpoint `/ready` pour distinguer l’état “process vivant” et “service prêt”.
- Endpoint `/metrics` enrichi avec la capacité et l’activité du worker.
- Script `scripts/bootstrap_config.py` pour générer un `config.yaml` prérempli à partir de l’environnement détecté.
- Workflow GitHub Actions pour exécuter la suite pytest.
- `SECURITY.md` pour documenter le signalement et le périmètre sécurité.
- L’API `POST /api/jobs/<id>/process` planifie désormais le traitement en arrière-plan et répond immédiatement.
- Les transitions de workflow liées au pré-traitement et au lancement de traitement sont centralisées.
- Le logging structuré conserve le contexte `job_id` et `step` hors requête Flask.
- La documentation active a été réalignée sur le mode service longue durée.

### Fixed
- Warning explicite lors de la création du premier admin si le mot de passe initial reste `admin-change-me`, `CHANGE-ME` ou vide.
- `_apply_llm_suggestions()` : faux positif silencieux — le test `”indisponible” in summary_text.lower()` déclenchait un early return quand le résumé LLM mentionnait légitimement le mot “indisponible” dans son contenu (ex : “fallback quand X est indisponible”), laissant `meeting_context.json` non mis à jour malgré un résumé valide. Remplacé par une comparaison exacte à la sentinelle `”Résumé indisponible.”`.
- `_apply_llm_suggestions()` : suppression du double header `# Résumé de contrôle` dans `summary.md` — opencode écrit ce header lui-même, `_apply_llm_suggestions` n’ajoute plus que l’extrait de transcription en fin de fichier (et seulement s’il est non vide).
- Régression de supervision causée par l’absence de `/health` et `/metrics`.
- Gestion plus sûre des relances de jobs bloqués et des annulations.
