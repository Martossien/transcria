# Configuration de production (contient des secrets et chemins absolus)
config.yaml

# Runtime data
instance/
/jobs/
voices/
*.db
.transcria_pids

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/

# Pytest
.pytest_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
venv/
.venv/

# Local generated install/runtime artifacts
configs/local/
scripts/generated/

# Logs
*.log

# Audit interne (document de travail)
AUDIT_INTERNE.md

# Agent internal data
.claude/
.agents/
.codex/

# OS
.DS_Store
Thumbs.db

# Models (trop volumineux pour git, mais garder l'arborescence)
models/cohere-asr/*
!models/cohere-asr/.gitkeep
models/granite-speech-4.1-2b/
models/sortformer-4spk-v2.1/
models/voxtral-mini-3b-2507/

# Résultats de benchmark (données de run, non versionnées)
bench_results/

# Backups de développement (historique de refactoring)
backup/

# Documentation interne (plans de refactoring, archives historiques)
docs/plan_refactoring_*.md
docs/archive/

# Documents de conception internes (ne pas publier) — désormais sous docs/archive/
docs/private/
docs/STT_CORPUS.md


# Archives (benchmarks, audio tests, docs historiques, scripts obsolètes)
archives/

# Fichiers audio volumineux (sauf sons de test unitaires)
*.wav
*.m4a
*.flac
*.ogg
# mp3 ignorés sauf les fichiers de test E2E
*.mp3
!tests/test1.mp3
!tests/test2.mp3
.coverage
coverage.xml

# Backups générés par install.sh
config.yaml.bak.*
backups/
.env.split
docker-compose.split-gpu.dev.yml
.env.docker
config.restored.yaml
backups/

# Catalogues i18n : le .pot et les .mo sont GÉNÉRÉS (extract/compile en CI + build Docker
# + entrypoint + install), jamais un binaire périmé en git. On versionne uniquement les .po.
transcria/web/translations/messages.pot
transcria/web/translations/**/*.mo
models/kroko/
models/parakeet-cpp/
runtimes/
docker-compose.legacy-gpu.yml
