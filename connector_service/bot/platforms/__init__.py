"""Drivers navigateur par plateforme (Playwright) — implémentent `BrowserDriver`. Gate manuel.

Banc d'essai = Jitsi (public, sans compte). Zoom-web / Teams / Google Meet réutilisent le même
`BrowserDriver` + payload de capture, en ne changeant que les sélecteurs d'UI et le join.
"""
