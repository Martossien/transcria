flask>=3.0,<4.0
flask-login>=0.6,<1.0
# Chantier identité (docs/GESTION_IDENTITE.md) : client OIDC (lot 1).
authlib>=1.7,<2.0
# Chantier identité : connecteur LDAP / Active Directory direct (lot 2). Pur-Python
# (tire pyasn1) → images Docker inchangées ; importé de façon différée (aucun coût
# pour les installations en backend local/oidc/proxy).
ldap3>=2.9,<3.0
flask-sqlalchemy>=3.1,<4.0
# i18n (interface multilingue FR/EN) : gettext + négociation Accept-Language + formats
# date/nombre localisés. Pur-Python (tire Babel) → images Docker inchangées. Catalogues
# dans transcria/web/translations/, compilés en CI + build Docker + entrypoint.
flask-babel>=4.0,<5.0
werkzeug>=3.0,<4.0
gunicorn>=21.2,<24.0        # serveur WSGI du tier web (rôle web/all en prod)
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0
requests>=2.31,<3.0
sqlalchemy>=2.0,<3.0

# Base de données
psycopg[binary]>=3.2,<4.0   # driver PostgreSQL (prod)
alembic>=1.13,<2.0          # migrations de schéma

# Audio processing
librosa>=0.10,<0.12
soundfile>=0.12,<1.0
torchaudio>=2.1,<3.0
numpy>=1.26,<3.0
scipy>=1.10,<2.0
onnxruntime>=1.13,<2.0
demucs>=4.0,<5.0

# STT engines
torch>=2.1,<3.0
transformers>=4.52.1,<5.0
accelerate>=0.24,<2.0
faster-whisper>=1.2,<2.0
nemo_toolkit[asr]>=2.7,<3.0
# Tokenizer audio du backend STT expérimental Voxtral (Mistral, Apache-2.0).
mistral-common[audio]>=1.8,<2.0
# Backend STT kroko (Banafo Kroko-ASR, zipformer2 streaming) : seul backend 100 % CPU —
# fonctionne sans GPU. Wheel autonome (onnxruntime embarqué), ~20 Mo.
sherpa-onnx>=1.12,<2.0

# Speaker diarization
pyannote.audio>=4.0,<5.0
# Décodeur audio requis par pyannote.audio 4.x (AudioDecoder). Couplé à l'ABI de torch
# (torchcodec 0.12 ↔ torch 2.12) → installé depuis l'index torch par l'install/Docker pour
# garantir un wheel apparié ; requiert aussi les libs FFmpeg système au runtime (déjà exigées).
torchcodec>=0.12,<1.0

# Model cache (pin <1.0 : transformers 4.x bloque au import >=1.0)
huggingface-hub>=0.34,<1.0
hf-transfer>=0.1.6  # téléchargements HF multi-flux (repli automatique si échec ; TRANSCRIA_NO_HF_TRANSFER=1 pour couper)

# Document export
python-docx>=1.1,<2.0
# Extraction de texte des documents joints (support de réunion → contexte du résumé).
# Formats XML modernes uniquement, 100 % pur-Python (aucune dépendance système) :
# .docx via python-docx (ci-dessus), .pdf via pypdf, .pptx via python-pptx (tire Pillow/lxml,
# déjà présents). Les .doc/.ppt binaires hérités ne sont pas gérés (message dans l'UI).
pypdf>=5,<6
python-pptx>=1,<2
psutil>=5.9        # métriques CPU/RAM de la page Système (source locale, C2.3)
